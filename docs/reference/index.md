# API Reference

The API Reference documents DialogForge's public Python surface for operators and contributors.

Use the navigation tree to move from package-level entries to concrete modules and symbols.
Each page focuses on callable contracts: expected inputs, return values, side effects, and failure conditions.
