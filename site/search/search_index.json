{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p>Synthetic multi-turn dialogue generation for grounded datasets, with one CLI for local and distributed runs.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Grounded user-assistant conversation generation with retrieval and optional web search.</li> <li>Configurable online/offline judging, deterministic dedup, and resumable run state.</li> <li>Export-ready artifacts for ShareGPT-style datasets and Hugging Face workflows.</li> <li>Local mode and distributed mode support (Ray + Postgres + optional managed vLLM).</li> </ul>"},{"location":"#get-started","title":"Get Started","text":"<p>Get Started Setup / Installation</p>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Configuration</li> <li>CLI / Usage</li> <li>API Reference</li> <li>Contributing, Issues, and Pull Requests</li> </ul>"},{"location":"API_INDEX/","title":"DialogForge API Index (Curated)","text":""},{"location":"API_INDEX/#purpose","title":"Purpose","text":"<p>This index documents the stable, practical API surface for operators and contributors. It focuses on module responsibilities, public entrypoints, side effects, and call relationships.</p>"},{"location":"API_INDEX/#external-entrypoints","title":"External entrypoints","text":""},{"location":"API_INDEX/#cli-srcdlgforgeclipy","title":"CLI (<code>src/dlgforge/cli.py</code>)","text":"<ul> <li><code>build_parser() -&gt; argparse.ArgumentParser</code></li> <li><code>main() -&gt; None</code></li> </ul> <p>Commands routed by <code>main</code>: - <code>run</code> -&gt; <code>pipeline.runner.run</code> - <code>judge</code> -&gt; <code>pipeline.runner.run_judge_only</code> - <code>push</code> -&gt; <code>pipeline.hf_push.run_push</code> - <code>seeds-migrate</code> -&gt; <code>pipeline.seed_topics_migration.run_seeds_migrate</code></p>"},{"location":"API_INDEX/#package-exports-srcdlgforge__init__py","title":"Package exports (<code>src/dlgforge/__init__.py</code>)","text":"<ul> <li><code>run</code></li> <li><code>run_judge_only</code></li> <li><code>run_push</code></li> <li><code>run_seeds_migrate</code></li> <li><code>HFPushOptions</code></li> </ul>"},{"location":"API_INDEX/#configuration-api","title":"Configuration API","text":""},{"location":"API_INDEX/#srcdlgforgeconfigdefaultspy","title":"<code>src/dlgforge/config/defaults.py</code>","text":"<ul> <li><code>DEFAULT_CONFIG</code>: canonical default schema baseline.</li> </ul>"},{"location":"API_INDEX/#srcdlgforgeconfigloaderpy","title":"<code>src/dlgforge/config/loader.py</code>","text":"<p>Public resolver contract: - <code>load_config</code> - <code>build_base_inputs</code> - <code>resolve_output_dir</code> - <code>resolve_output_columns</code> - <code>resolve_target_languages</code> - <code>resolve_n_turns</code>, <code>resolve_turn_range</code>, <code>resolve_turn_count_distribution</code>, <code>resolve_turn_count_mean</code> - <code>resolve_total_samples</code>, <code>resolve_batch_size</code> - <code>resolve_distributed_enabled</code> - <code>resolve_retrieval_default_k</code> - <code>resolve_seed_topics_path</code>, <code>resolve_seed_topics_variant</code>, <code>resolve_seed_topics_probability</code>, <code>resolve_seed_topics_enabled</code> - <code>resolve_judge_mode</code>, <code>resolve_judge_enabled</code>, <code>resolve_judge_granularity</code>, <code>resolve_judge_reasons</code></p> <p>Side effects: - file read (<code>config.yaml</code>) - environment override application into in-memory config object</p>"},{"location":"API_INDEX/#srcdlgforgeconfigpersonaspy","title":"<code>src/dlgforge/config/personas.py</code>","text":"<p>Public contract: - <code>UniformPersonaSampler.sample</code> - <code>select_personas</code> - <code>build_uniform_persona_sampler</code> - <code>load_personas</code> - persona resolver helpers (<code>resolve_personas_enabled</code>, <code>resolve_personas_path</code>, <code>resolve_question_seed</code>)</p> <p>Side effects: - persona YAML file reads with fallback path handling</p>"},{"location":"API_INDEX/#pipeline-api","title":"Pipeline API","text":""},{"location":"API_INDEX/#srcdlgforgepipelinerunnerpy","title":"<code>src/dlgforge/pipeline/runner.py</code>","text":"<p>Primary orchestration entrypoints: - <code>run(config_path)</code> - <code>run_judge_only(config_path)</code></p> <p>Advanced orchestration helpers used internally and by tests: - <code>run_multi_turn</code> - <code>run_multi_turn_batched_async</code> - <code>persist_training_sample</code> - <code>persist_batched_training_samples</code></p> <p>Side effects: - network/model calls - retrieval and optional web tool invocations - output artifact writes - run-state checkpoint writes - optional distributed bootstrap handoff</p>"},{"location":"API_INDEX/#srcdlgforgepipelinesamplingpy","title":"<code>src/dlgforge/pipeline/sampling.py</code>","text":"<p>Sampling and memory contracts: - <code>build_question_inputs</code> - <code>select_question_mode</code> - <code>sample_topic_snippets</code> - <code>update_coverage_ledger</code> - <code>is_duplicate_question</code> - <code>update_doc_question_memory</code> - seed-topic helpers (<code>load_seed_topics</code>, <code>select_seed_candidate</code>, <code>update_seed_memory</code>)</p> <p>Side effects: - coverage ledger writes through IO layer - seed-topic file reads</p>"},{"location":"API_INDEX/#srcdlgforgepipelinestatepy","title":"<code>src/dlgforge/pipeline/state.py</code>","text":"<p>Run-state contract: - <code>init_run_state</code>, <code>checkpoint_run_state</code> - <code>init_batched_run_state</code>, <code>checkpoint_batched_run_state</code> - <code>build_initial_batched_conversations</code>, <code>load_batched_conversations_from_state</code></p> <p>Side effects: - run-state JSON writes/reads</p>"},{"location":"API_INDEX/#srcdlgforgepipelinehistorypy","title":"<code>src/dlgforge/pipeline/history.py</code>","text":"<p>History formatting contract: - <code>format_history</code> - <code>build_conversation_history</code> - <code>build_public_history</code> - <code>messages_up_to_turn</code></p>"},{"location":"API_INDEX/#srcdlgforgepipelinepromptspy","title":"<code>src/dlgforge/pipeline/prompts.py</code>","text":"<p>Prompt loading/rendering contract: - <code>load_agents_config</code> - <code>load_tasks_config</code> - <code>build_agent_system_prompt</code> - <code>build_task_prompt</code></p> <p>Side effects: - prompt YAML file reads with LRU caching</p>"},{"location":"API_INDEX/#srcdlgforgepipelinededuppy","title":"<code>src/dlgforge/pipeline/dedup.py</code>","text":"<p>Dedup contract: - <code>normalize_question</code> - <code>RunQuestionRegistry.filter_and_commit</code></p>"},{"location":"API_INDEX/#srcdlgforgepipelineseed_topics_migrationpy","title":"<code>src/dlgforge/pipeline/seed_topics_migration.py</code>","text":"<p>Seed migration contract: - <code>run_seeds_migrate</code> - <code>migrate_seed_topics_file</code></p> <p>Side effects: - seed file read/write and destination creation</p>"},{"location":"API_INDEX/#srcdlgforgepipelinehf_pushpy","title":"<code>src/dlgforge/pipeline/hf_push.py</code>","text":"<p>Export/push contract: - dataclasses: <code>HFPushSettings</code>, <code>HFPushOptions</code> - <code>run_push</code> - <code>maybe_auto_push_after_run</code> - <code>resolve_hf_push_settings</code> - <code>prepare_export</code> - <code>push_to_hub</code></p> <p>Side effects: - export directory file writes - optional Hugging Face network push</p>"},{"location":"API_INDEX/#llm-api","title":"LLM API","text":""},{"location":"API_INDEX/#srcdlgforgellmsettingspy","title":"<code>src/dlgforge/llm/settings.py</code>","text":"<ul> <li><code>resolve_llm_settings</code></li> <li><code>resolve_agent_used_name</code></li> <li><code>required_agents</code></li> <li><code>missing_models</code></li> </ul>"},{"location":"API_INDEX/#srcdlgforgellmclientpy","title":"<code>src/dlgforge/llm/client.py</code>","text":"<ul> <li>dataclass <code>ChatResult</code></li> <li>client <code>OpenAIModelClient.complete</code></li> <li>client <code>OpenAIModelClient.acomplete</code></li> </ul> <p>Side effects: - outbound API calls - routing state mutation for weighted least-inflight logic</p>"},{"location":"API_INDEX/#tooling-api","title":"Tooling API","text":""},{"location":"API_INDEX/#srcdlgforgetoolsretrievalpy","title":"<code>src/dlgforge/tools/retrieval.py</code>","text":"<ul> <li>class <code>KnowledgeVectorStore</code> with retrieval/listing/sample methods</li> <li><code>configure_retrieval</code></li> <li><code>get_vector_store</code></li> <li><code>vector_db_search</code></li> </ul> <p>Side effects: - index build/read on filesystem - embedding model initialization</p>"},{"location":"API_INDEX/#srcdlgforgetoolsweb_searchpy","title":"<code>src/dlgforge/tools/web_search.py</code>","text":"<ul> <li><code>SerperWebSearchClient.search</code></li> </ul> <p>Side effects: - outbound HTTP requests to Serper endpoint</p>"},{"location":"API_INDEX/#io-and-utility-api","title":"IO and utility API","text":""},{"location":"API_INDEX/#srcdlgforgeiooutputpy","title":"<code>src/dlgforge/io/output.py</code>","text":"<p>Public output contract: - <code>OutputPaths</code> - <code>configure_output_columns</code> - <code>ensure_output_layout</code> - <code>load_coverage_ledger</code>, <code>append_coverage_ledger</code> - <code>save_run_state</code>, <code>load_run_state</code> - <code>save_training_sample</code> - <code>append_sharegpt_judged_record</code></p>"},{"location":"API_INDEX/#srcdlgforgeutilspy","title":"<code>src/dlgforge/utils/*.py</code>","text":"<ul> <li>env parsing: <code>env_flag</code>, <code>env_int</code>, <code>env_float</code>, <code>load_dotenv_files</code></li> <li>JSON parsing: <code>strip_code_fences</code>, <code>extract_json_object</code>, <code>parse_json_object</code></li> <li>logging setup: <code>setup_logging</code></li> <li>config/path merges: <code>deep_merge</code>, <code>resolve_path</code></li> <li>text helpers: <code>hash_text</code>, <code>render_template</code></li> </ul>"},{"location":"API_INDEX/#distributed-runtime-api","title":"Distributed runtime API","text":""},{"location":"API_INDEX/#srcdlgforgedistributedbootstrappy","title":"<code>src/dlgforge/distributed/bootstrap.py</code>","text":"<ul> <li>class <code>RunBootstrap.run</code></li> <li>function <code>run_bootstrap</code></li> </ul>"},{"location":"API_INDEX/#srcdlgforgedistributedprovisioningpy","title":"<code>src/dlgforge/distributed/provisioning.py</code>","text":"<ul> <li>protocol <code>VLLMProvisioner</code></li> <li>implementations: <code>NoopProvisioner</code>, <code>AttachProvisioner</code>, <code>ManagedRayVLLMProvisioner</code></li> </ul>"},{"location":"API_INDEX/#srcdlgforgedistributedray_runtimepy","title":"<code>src/dlgforge/distributed/ray_runtime.py</code>","text":"<ul> <li><code>import_ray</code></li> <li><code>create_worker_actor</code></li> <li><code>create_coordinator_actor</code></li> <li><code>create_vllm_server_actor</code></li> </ul>"},{"location":"API_INDEX/#srcdlgforgedistributedtypespy","title":"<code>src/dlgforge/distributed/types.py</code>","text":"<ul> <li>dataclass <code>EndpointSpec</code></li> <li><code>EndpointSpec.to_routing_dict</code></li> </ul>"},{"location":"API_INDEX/#cross-module-call-relationships","title":"Cross-module call relationships","text":"<ul> <li><code>cli</code> -&gt; <code>pipeline.runner</code> / <code>pipeline.hf_push</code> / <code>pipeline.seed_topics_migration</code></li> <li><code>pipeline.runner</code> -&gt; <code>config</code>, <code>pipeline.sampling</code>, <code>pipeline.prompts</code>, <code>llm</code>, <code>tools</code>, <code>io</code>, <code>pipeline.state</code>, <code>pipeline.hf_push</code></li> <li><code>pipeline.sampling</code> -&gt; <code>tools.retrieval</code>, <code>io.output</code></li> <li><code>distributed.bootstrap</code> -&gt; <code>distributed.provisioning</code> + <code>distributed.ray_runtime</code> -&gt; <code>pipeline.runner</code></li> <li><code>pipeline.hf_push</code> -&gt; <code>config</code> + <code>io</code> + optional Hub API</li> </ul>"},{"location":"API_INDEX/#package-export-map-__all__","title":"Package export map (<code>__all__</code>)","text":""},{"location":"API_INDEX/#dlgforge","title":"<code>dlgforge</code>","text":"<ul> <li><code>run</code>, <code>run_judge_only</code>, <code>run_push</code>, <code>run_seeds_migrate</code>, <code>HFPushOptions</code></li> </ul>"},{"location":"API_INDEX/#dlgforgeconfig","title":"<code>dlgforge.config</code>","text":"<ul> <li><code>DEFAULT_CONFIG</code>, <code>load_config</code>, <code>build_base_inputs</code>, resolver helpers</li> </ul>"},{"location":"API_INDEX/#dlgforgepipeline","title":"<code>dlgforge.pipeline</code>","text":"<ul> <li><code>run</code>, <code>run_judge_only</code>, <code>run_push</code>, <code>run_seeds_migrate</code>, <code>HFPushOptions</code></li> </ul>"},{"location":"API_INDEX/#dlgforgellm","title":"<code>dlgforge.llm</code>","text":"<ul> <li><code>ChatResult</code>, <code>OpenAIModelClient</code>, settings helpers</li> </ul>"},{"location":"API_INDEX/#dlgforgeio","title":"<code>dlgforge.io</code>","text":"<ul> <li><code>OutputPaths</code> and output persistence helpers</li> </ul>"},{"location":"API_INDEX/#dlgforgetools","title":"<code>dlgforge.tools</code>","text":"<ul> <li>retrieval config/search helpers and <code>SerperWebSearchClient</code></li> </ul>"},{"location":"API_INDEX/#dlgforgedistributed","title":"<code>dlgforge.distributed</code>","text":"<ul> <li>bootstrap/provisioning types and helpers</li> </ul>"},{"location":"API_INDEX/#dlgforgeutils","title":"<code>dlgforge.utils</code>","text":"<ul> <li>env/json/logging/merge/text helper functions</li> </ul>"},{"location":"ARCHITECTURE/","title":"Architecture","text":""},{"location":"ARCHITECTURE/#system-purpose","title":"System purpose","text":"<p>DialogForge generates grounded synthetic multi-turn conversations with deterministic dedup behavior, resumable run state, and export-ready outputs. The system is built around a small CLI contract and a modular pipeline that can run locally or through distributed bootstrap.</p>"},{"location":"ARCHITECTURE/#end-to-end-execution-flow","title":"End-to-end execution flow","text":"<pre><code>flowchart TD\n  A[\"dlgforge run config.yaml\"] --&gt; B[\"Load config (defaults + YAML + env overrides)\"]\n  B --&gt; C{\"run.distributed.enabled\"}\n  C --&gt;|false| D[\"Initialize local pipeline runtime\"]\n  C --&gt;|true| E[\"RunBootstrap (Ray + Postgres + backend checks)\"]\n  E --&gt; F[\"Coordinator actor executes generation run\"]\n  D --&gt; G[\"Configure retrieval + base inputs\"]\n  F --&gt; G\n  G --&gt; H[\"Run generation waves per target language\"]\n  H --&gt; I[\"Persist outputs + run_state\"]\n  I --&gt; J[\"Optional HF auto-push/export\"]</code></pre>"},{"location":"ARCHITECTURE/#turn-pipeline","title":"Turn pipeline","text":"<pre><code>flowchart LR\n  Q[\"qa_generator\"] --&gt; R[\"kb_responder\"]\n  R --&gt; T{\"judge.enabled\"}\n  T --&gt;|false| U[\"Persist turn/conversation\"]\n  T --&gt;|true + turn| V[\"qa_judge per turn\"]\n  T --&gt;|true + conversation| W[\"conversation-level judge after final turn\"]\n  V --&gt; U\n  W --&gt; U</code></pre>"},{"location":"ARCHITECTURE/#distributed-bootstrap-sequence","title":"Distributed bootstrap sequence","text":"<pre><code>flowchart TD\n  A[\"RunBootstrap\"] --&gt; B[\"Initialize Ray runtime\"]\n  B --&gt; C[\"Validate Postgres DSN and connectivity\"]\n  C --&gt; D{\"llm.backend\"}\n  D --&gt;|openai| E[\"No vLLM provisioning\"]\n  D --&gt;|vllm_attach| F[\"Validate configured endpoint health\"]\n  D --&gt;|vllm_managed| G[\"Provision managed vLLM server actors\"]\n  E --&gt; H[\"Spawn coordinator + workers\"]\n  F --&gt; H\n  G --&gt; H\n  H --&gt; I[\"Execute generation workflow\"]</code></pre>"},{"location":"ARCHITECTURE/#core-module-boundaries","title":"Core module boundaries","text":"<ul> <li><code>src/dlgforge/cli.py</code>: external command surface and dispatch.</li> <li><code>src/dlgforge/config</code>: config defaults, loader, and resolver layer.</li> <li><code>src/dlgforge/pipeline/runner.py</code>: top-level generation orchestration.</li> <li><code>src/dlgforge/pipeline/sampling.py</code>: question selection, coverage memory, and seed-topic mechanics.</li> <li><code>src/dlgforge/tools/retrieval.py</code>: vector index lifecycle and retrieval operations.</li> <li><code>src/dlgforge/io/output.py</code>: output paths and artifact writing.</li> <li><code>src/dlgforge/pipeline/state.py</code>: resume/checkpoint state handling.</li> <li><code>src/dlgforge/distributed</code>: bootstrap and backend provisioning abstractions.</li> <li><code>src/dlgforge/pipeline/hf_push.py</code>: export packaging and hub push flow.</li> </ul>"},{"location":"ARCHITECTURE/#data-and-state-model","title":"Data and state model","text":"<ul> <li>Conversation and turn artifacts are written under <code>saving.output_dir</code>.</li> <li>Run progress is checkpointed in <code>run_state</code> files keyed by <code>run_id</code>.</li> <li>Dedup/coverage memory is tracked in append-oriented ledgers.</li> <li>Resume uses persisted run-state and memory artifacts to continue without replaying accepted outputs.</li> </ul>"},{"location":"ARCHITECTURE/#stability-model","title":"Stability model","text":"<ul> <li>Stable operator-facing contracts in <code>v0.1.x</code>: CLI commands, documented config surfaces, and output layouts.</li> <li>Internal module structure under <code>src/dlgforge</code> is not a stability contract.</li> </ul>"},{"location":"CLI_REFERENCE/","title":"CLI Reference","text":""},{"location":"CLI_REFERENCE/#command-surface","title":"Command surface","text":"<p><code>dlgforge</code> exposes four commands: - <code>run</code>: execute generation pipeline. - <code>judge</code>: run judge-only pass on existing conversations. - <code>push</code>: prepare export and optionally push to Hugging Face Hub. - <code>seeds-migrate</code>: migrate legacy seed topic files into structured YAML format.</p>"},{"location":"CLI_REFERENCE/#dlgforge-run-config","title":"<code>dlgforge run &lt;config&gt;</code>","text":"<p>Runs generation from YAML config.</p> <p>Behavior summary: - validates config path. - sets up logging and dotenv loading. - resolves config and optional distributed bootstrap. - runs generation and persistence.</p> <p>Exit behavior: - exits non-zero with <code>dlgforge run failed: &lt;error&gt;</code> on unhandled runtime errors.</p>"},{"location":"CLI_REFERENCE/#dlgforge-judge-config","title":"<code>dlgforge judge &lt;config&gt;</code>","text":"<p>Runs judge logic over existing conversation artifacts.</p> <p>Use when: - conversations were generated without online judge. - judge prompt/model settings changed and you need a re-pass.</p> <p>Exit behavior: - exits non-zero with <code>dlgforge judge failed: &lt;error&gt;</code> on failure.</p>"},{"location":"CLI_REFERENCE/#dlgforge-push-config-options","title":"<code>dlgforge push &lt;config&gt; [options]</code>","text":"<p>Exports dataset artifacts and optionally pushes to the hub.</p> <p>Key options: - <code>--repo-id</code> - <code>--repo-type {dataset,model,space}</code> - <code>--source-dir</code> - <code>--export-dir</code> - <code>--include-run-state</code> - <code>--token</code> - <code>--commit-message</code> - <code>--no-export</code> - <code>--no-push</code> - <code>--clean-remote</code></p> <p>Exit behavior: - exits non-zero with <code>dlgforge push failed: &lt;error&gt;</code> on failure.</p>"},{"location":"CLI_REFERENCE/#dlgforge-seeds-migrate-config-options","title":"<code>dlgforge seeds-migrate &lt;config&gt; [options]</code>","text":"<p>Migrates legacy seed topics into scalable YAML schema.</p> <p>Options: - <code>--source-file</code> - <code>--dest-file</code> - <code>--overwrite</code></p> <p>Exit behavior: - exits non-zero with <code>dlgforge seeds-migrate failed: &lt;error&gt;</code> on failure.</p>"},{"location":"CLI_REFERENCE/#invocation-forms","title":"Invocation forms","text":"<p>Equivalent entry forms: <pre><code>dlgforge run config.yaml\nuv run dlgforge run config.yaml\npython -m dlgforge run config.yaml\n</code></pre></p>"},{"location":"CLI_REFERENCE/#operator-notes","title":"Operator notes","text":"<ul> <li>In distributed mode, <code>run</code> may bootstrap Ray/Postgres/vLLM lifecycle before generation starts.</li> <li><code>push --no-push</code> is useful for local export validation in CI/release pipelines.</li> </ul>"},{"location":"CONFIG_REFERENCE/","title":"Configuration Reference","text":""},{"location":"CONFIG_REFERENCE/#resolution-and-precedence","title":"Resolution and precedence","text":"<p>Config values are resolved in this order: 1. <code>DEFAULT_CONFIG</code> (<code>src/dlgforge/config/defaults.py</code>). 2. User YAML (<code>config.yaml</code>) deep-merged over defaults. 3. Environment overrides applied in <code>load_config()</code>. 4. Runtime-specific environment overrides applied later by distributed bootstrap (for routed endpoints and managed model wiring). 5. LLM per-agent/global env resolution inside <code>resolve_llm_settings()</code> at call time.</p>"},{"location":"CONFIG_REFERENCE/#run","title":"<code>run</code>","text":"<ul> <li><code>n_turns</code>: fixed turn count fallback.</li> <li><code>batch_size</code>: number of conversations advanced concurrently.</li> <li><code>total_samples</code>: target conversations per language.</li> <li><code>target_languages</code>: primary language loop. If missing, falls back to legacy <code>target_language</code>, then <code>en</code>.</li> <li><code>min_turns</code>/<code>max_turns</code>: sampled range. If both are unset/invalid, fixed <code>n_turns</code> is used.</li> <li><code>turn_count_distribution</code>: <code>uniform|poisson|exponential</code> (aliases accepted for some values).</li> <li><code>turn_count_mean</code>: positive float used by <code>poisson</code>/<code>exponential</code>.</li> <li><code>seed_question</code> and <code>question_seed</code>: deterministic sampling context.</li> <li><code>run_id</code> and <code>resume_run_id</code>: explicit run identity and resume checkpoint.</li> <li><code>seed_topics_*</code>: seed topic path/variant/probability/enable flags.</li> <li><code>distributed.enabled</code>: toggles bootstrap path.</li> </ul> <p>Validation details: - Invalid <code>min_turns &gt; max_turns</code> raises <code>ValueError</code>. - <code>total_samples &lt; 0</code> falls back to configured default.</p>"},{"location":"CONFIG_REFERENCE/#llm","title":"<code>llm</code>","text":"<ul> <li><code>backend</code>: <code>openai|vllm_attach|vllm_managed</code>.</li> <li>Shared keys: <code>provider</code>, <code>model</code>, <code>base_url</code>, <code>api_key</code>, <code>api_key_env</code>, <code>temperature</code>, <code>max_tokens</code>, <code>top_p</code>, <code>timeout</code>, <code>max_retries</code>, <code>extra</code>.</li> <li><code>agents.&lt;agent&gt;</code> overrides shared keys per logical agent.</li> <li><code>routing</code>: endpoint list and strategy for routed execution.</li> <li><code>vllm</code>: managed-server settings used when backend is managed.</li> </ul> <p>Per-agent model selection uses: - agent-specific env <code>LLM_&lt;AGENT&gt;_*</code> - global env <code>LLM_*</code> - <code>OPENAI_*</code> fallback for key/base URL/model</p>"},{"location":"CONFIG_REFERENCE/#ray","title":"<code>ray</code>","text":"<ul> <li><code>address</code>: Ray address, commonly <code>auto</code>.</li> <li><code>auto_start_local</code>: permits local fallback when <code>address=auto</code> and no cluster found.</li> <li><code>namespace</code>: Ray namespace.</li> <li><code>actor.*</code>: CPU/GPU and replica settings for coordinator/worker actors.</li> </ul>"},{"location":"CONFIG_REFERENCE/#store","title":"<code>store</code>","text":"<ul> <li><code>backend</code>: currently distributed flow expects <code>postgres</code>.</li> <li><code>postgres.dsn</code>: required for distributed run.</li> <li>pool and timeout keys tune DB client behavior.</li> </ul>"},{"location":"CONFIG_REFERENCE/#retrieval","title":"<code>retrieval</code>","text":"<ul> <li><code>default_k</code>: retrieval depth.</li> <li><code>chunk_size</code>, <code>overlap</code>: chunking controls.</li> <li><code>persist_dir</code>, <code>rebuild_index</code>, <code>skip_if_unchanged</code>: index persistence policy.</li> <li>embedding backend/model kwargs are configurable and can be env-overridden.</li> </ul>"},{"location":"CONFIG_REFERENCE/#coverage","title":"<code>coverage</code>","text":"<ul> <li>controls document balancing and dedup retry budget.</li> <li><code>question_dedup_retries</code> directly impacts drop probability in high-collision datasets.</li> </ul>"},{"location":"CONFIG_REFERENCE/#tools","title":"<code>tools</code>","text":"<ul> <li><code>web_search_enabled</code>: toggles tool availability in assistant stage.</li> <li><code>serper_num_results</code>, <code>serper_timeout</code>: Serper query controls.</li> </ul>"},{"location":"CONFIG_REFERENCE/#personas","title":"<code>personas</code>","text":"<ul> <li><code>enabled</code>: persona injection on/off.</li> <li><code>path</code>: YAML persona source path.</li> <li>if unavailable, built-in fallback personas are used.</li> </ul>"},{"location":"CONFIG_REFERENCE/#judge","title":"<code>judge</code>","text":"<ul> <li><code>mode</code>: <code>online|offline</code>.</li> <li><code>enabled</code>: additional gate.</li> <li><code>granularity</code>: <code>turn|conversation</code>.</li> <li><code>reasons</code>: allowed labels for judge output.</li> </ul> <p>Important behavior: - <code>resolve_judge_enabled()</code> returns <code>False</code> when mode is <code>offline</code> even if <code>enabled=true</code>.</p>"},{"location":"CONFIG_REFERENCE/#saving","title":"<code>saving</code>","text":"<ul> <li><code>output_dir</code>: artifact root.</li> <li><code>output_columns</code>: ShareGPT judged export column mapping.</li> <li>alias <code>message_with_tools</code> is normalized to <code>messages_with_tools</code>.</li> <li><code>hf_push</code>: export/push toggles and metadata.</li> </ul>"},{"location":"CONFIG_REFERENCE/#environment-override-keys-selected","title":"Environment override keys (selected)","text":"<p>Common high-impact env keys handled in <code>load_config()</code>: - run: <code>N_TURNS</code>, <code>BATCH_SIZE</code>, <code>TOTAL_SAMPLES</code>, <code>MIN_TURNS</code>, <code>MAX_TURNS</code>, <code>TARGET_LANGUAGES</code>, <code>RUN_ID</code>, <code>RESUME_RUN_ID</code> - distributed/ray: <code>DISTRIBUTED_ENABLED</code>, <code>RAY_ADDRESS</code>, <code>RAY_AUTO_START_LOCAL</code> - retrieval: <code>KB_DEFAULT_K</code>, <code>KB_CHUNK_SIZE</code>, <code>KB_PERSIST_DIR</code>, embedding kwargs JSON keys - judge: <code>JUDGE_MODE</code>, <code>JUDGE_GRANULARITY</code>, <code>JUDGE_ENABLED</code> - output/export: <code>OUTPUT_DIR</code>, <code>HF_PUSH_*</code> - llm routing: <code>LLM_BACKEND</code>, <code>LLM_ROUTING_STRATEGY</code>, <code>LLM_ROUTING_ENDPOINTS_JSON</code></p> <p>For the full source of truth, use <code>src/dlgforge/config/loader.py</code> and <code>src/dlgforge/llm/settings.py</code>.</p>"},{"location":"DISTRIBUTED_RUNTIME/","title":"Distributed Runtime","text":""},{"location":"DISTRIBUTED_RUNTIME/#overview","title":"Overview","text":"<p>Distributed execution is enabled by <code>run.distributed.enabled=true</code>. The runtime uses Ray actors for orchestration and requires Postgres for distributed state prerequisites.</p>"},{"location":"DISTRIBUTED_RUNTIME/#bootstrap-lifecycle","title":"Bootstrap lifecycle","text":"<p>Entry path: - <code>src/dlgforge/pipeline/runner.py</code> detects distributed mode. - <code>src/dlgforge/distributed/bootstrap.py</code> executes bootstrap.</p> <p>Bootstrap stages: 1. Initialize Ray runtime (<code>ray.address</code> with optional local fallback for <code>auto</code>). 2. Validate Postgres DSN and connectivity (<code>SELECT 1</code>). 3. Select provisioner based on <code>llm.backend</code>. 4. Optionally spawn worker and coordinator actors. 5. Execute generation under bootstrap env overrides. 6. Stop provisioned resources and optionally shutdown Ray started by bootstrap.</p>"},{"location":"DISTRIBUTED_RUNTIME/#backend-behavior","title":"Backend behavior","text":"<ul> <li><code>openai</code>: no vLLM provisioning; routed endpoint overrides are not required.</li> <li><code>vllm_attach</code>: validates configured endpoints (<code>/v1/models</code>) before run.</li> <li><code>vllm_managed</code>: starts vLLM server actors on Ray workers and waits until healthy.</li> </ul>"},{"location":"DISTRIBUTED_RUNTIME/#required-prerequisites","title":"Required prerequisites","text":"<ul> <li><code>store.backend=postgres</code> for distributed path.</li> <li>Postgres DSN provided by <code>store.postgres.dsn</code> or <code>DLGFORGE_POSTGRES_DSN</code>.</li> <li>Ray installed and reachable.</li> <li>For managed vLLM: <code>vllm</code> binary available on worker nodes.</li> </ul>"},{"location":"DISTRIBUTED_RUNTIME/#runtime-env-overrides-injected-by-bootstrap","title":"Runtime env overrides injected by bootstrap","text":"<p>Typical overrides set for the coordinator execution environment: - <code>DLGFORGE_RUN_BOOTSTRAPPED=1</code> - <code>LLM_BACKEND=&lt;backend&gt;</code> - <code>LLM_ROUTING_STRATEGY=&lt;strategy&gt;</code> (when endpoints exist) - <code>LLM_ROUTING_ENDPOINTS_JSON=&lt;json&gt;</code> (when endpoints exist) - <code>LLM_MODEL=&lt;served_model_name&gt;</code> in managed mode when model is otherwise unset</p>"},{"location":"DISTRIBUTED_RUNTIME/#failure-modes","title":"Failure modes","text":"<p>Common bootstrap failures: - Missing or invalid Postgres DSN. - Ray cluster unavailable and local fallback disabled. - <code>vllm_attach</code> endpoints unhealthy. - <code>vllm_managed</code> missing <code>vllm</code> binary or failing server startup.</p>"},{"location":"DISTRIBUTED_RUNTIME/#operational-recommendations","title":"Operational recommendations","text":"<ul> <li>Keep <code>ray.auto_start_local=true</code> for laptop/dev convenience.</li> <li>In production, use explicit Ray addresses and monitored Postgres.</li> <li>Validate endpoint health out-of-band before long runs.</li> <li>Keep <code>llm.vllm.served_model_name</code> aligned with agent model names in managed mode.</li> </ul>"},{"location":"HF_EXPORT/","title":"Hugging Face Export","text":""},{"location":"HF_EXPORT/#entry-points","title":"Entry points","text":"<p>Export/push behavior lives in <code>src/dlgforge/pipeline/hf_push.py</code>: - <code>run_push(config_path, options)</code> for explicit CLI push flow. - <code>maybe_auto_push_after_run(cfg, output_paths)</code> for automatic post-run export/push.</p>"},{"location":"HF_EXPORT/#export-phases","title":"Export phases","text":"<ol> <li>Resolve effective source/export directories.</li> <li>Prepare export bundle from generated artifacts.</li> <li>Sanitize rows for Hub compatibility.</li> <li>Generate dataset card (and optionally stats/plots).</li> <li>Optionally push to Hub via <code>huggingface_hub</code> API.</li> </ol>"},{"location":"HF_EXPORT/#key-configuration","title":"Key configuration","text":"<p><code>saving.hf_push.*</code> controls: - enable flags (<code>enabled</code>, <code>auto_push_on_run</code>) - destination (<code>repo_id</code>, <code>repo_type</code>, <code>private</code>) - source/export layout (<code>source_file</code>, <code>export_dir</code>, <code>include_run_state</code>) - metadata and cleanup (<code>commit_message</code>, <code>clean_remote</code>) - analytics outputs (<code>generate_stats</code>, <code>stats_file</code>, <code>generate_plots</code>, <code>plots_dir</code>)</p>"},{"location":"HF_EXPORT/#cli-options-override","title":"CLI options override","text":"<p><code>dlgforge push</code> options can override config for one-off runs: - <code>--repo-id</code>, <code>--repo-type</code>, <code>--source-dir</code>, <code>--export-dir</code> - <code>--include-run-state</code>, <code>--token</code>, <code>--commit-message</code> - <code>--no-export</code>, <code>--no-push</code>, <code>--clean-remote</code></p>"},{"location":"HF_EXPORT/#sanitization-behavior","title":"Sanitization behavior","text":"<p>Export preparation normalizes content to avoid downstream schema inconsistencies, including: - reasoning-trace shape normalization - null/list normalization for retrieval fields - thinking trace normalization to text-friendly structures</p> <p>These behaviors are covered by tests in <code>tests/test_hf_push_sanitization.py</code>.</p>"},{"location":"HF_EXPORT/#generated-extra-assets","title":"Generated extra assets","text":"<p>When enabled, export can include: - dataset stats JSON summary - SVG plots under configured plot directory - dataset card with source file and metadata</p>"},{"location":"HF_EXPORT/#operational-recommendations","title":"Operational recommendations","text":"<ul> <li>Use <code>--no-push</code> first to validate export bundle locally.</li> <li>Keep source file aligned with desired judge granularity (<code>conversations_sharegpt_judged.jsonl</code> for judged exports).</li> <li>Use explicit token input in CI where environment setup may vary.</li> </ul>"},{"location":"OUTPUTS_AND_SCHEMAS/","title":"Outputs and Schemas","text":""},{"location":"OUTPUTS_AND_SCHEMAS/#output-layout","title":"Output layout","text":"<p>Artifacts are created under <code>saving.output_dir</code> using <code>OutputPaths</code>.</p> <p>Primary files: - <code>synthetic_qa.jsonl</code> - <code>coverage_ledger.jsonl</code> - <code>turns.jsonl</code> - <code>conversations_sharegpt.jsonl</code> - <code>conversations_sharegpt_judged.jsonl</code> - <code>conversations/&lt;conversation_id&gt;.json</code> - <code>conversations_index.jsonl</code> - <code>run_state/&lt;run_id&gt;.json</code> - <code>run_state/last_run_id.txt</code></p>"},{"location":"OUTPUTS_AND_SCHEMAS/#conversation-dataset-row-synthetic_qajsonl","title":"Conversation dataset row (<code>synthetic_qa.jsonl</code>)","text":"<p>Typical fields: - <code>conversation_id</code>, <code>timestamp</code> - <code>question</code>, <code>inputs</code> - <code>qa_generation_plan</code>, <code>kb_final_answer</code>, <code>qa_judge</code>, <code>conversation_judge</code> - <code>turns</code>, <code>conversation_history</code>, <code>messages</code> - <code>raw_result</code>, <code>raw_results</code></p>"},{"location":"OUTPUTS_AND_SCHEMAS/#conversation-file-conversationsjson","title":"Conversation file (<code>conversations/*.json</code>)","text":"<p>Richer per-conversation artifact containing: - personas and language metadata - messages and tool-augmented messages - user/assistant reasoning slices - turn payloads and conversation-level judge - raw per-turn outputs</p>"},{"location":"OUTPUTS_AND_SCHEMAS/#turn-dataset-row-turnsjsonl","title":"Turn dataset row (<code>turns.jsonl</code>)","text":"<p>Flattened per-turn row with: - identifiers (<code>conversation_id</code>, <code>turn_index</code>, <code>timestamp</code>) - <code>user_message</code>, <code>assistant_message</code> - question mode and seed topic context - judge score/reasons when available</p>"},{"location":"OUTPUTS_AND_SCHEMAS/#sharegpt-exports","title":"ShareGPT exports","text":"<ul> <li><code>conversations_sharegpt.jsonl</code>: baseline export.</li> <li><code>conversations_sharegpt_judged.jsonl</code>: includes configurable columns for:</li> <li>messages</li> <li>messages with tools</li> <li>metadata</li> <li>user reasoning</li> <li>assistant reasoning</li> <li>judge payload</li> </ul> <p>Column names are remappable via <code>saving.output_columns</code>.</p>"},{"location":"OUTPUTS_AND_SCHEMAS/#coverage-ledger","title":"Coverage ledger","text":"<p><code>coverage_ledger.jsonl</code> stores dedup/coverage memory used by sampling: - question hashes - topic/document usage - seed-topic usage</p> <p>This ledger is read at runtime to preserve diversity and avoid duplicate questions.</p>"},{"location":"OUTPUTS_AND_SCHEMAS/#run-state-schema","title":"Run state schema","text":"<p>Run state snapshots include: - <code>run_id</code>, <code>status</code>, <code>updated_at</code> - <code>inputs</code> - <code>n_turns</code> - per-turn state and raw outputs</p> <p>Batched state additionally stores: - <code>batch_size</code> - per-slot conversation status (<code>active|completed|dropped</code>) - <code>drop_reason</code> for exhausted dedup slots</p>"},{"location":"OUTPUTS_AND_SCHEMAS/#resume-semantics","title":"Resume semantics","text":"<ul> <li><code>run.resume_run_id</code> loads matching state file when present.</li> <li>Batched resume requires requested <code>batch_size</code> to match stored state batch size.</li> <li>Slot <code>inputs</code> and turn history are restored from state.</li> <li>Coverage and dedup memory are rebuilt from existing persisted artifacts.</li> </ul>"},{"location":"RETRIEVAL_AND_PROMPTS/","title":"Retrieval and Prompts","text":""},{"location":"RETRIEVAL_AND_PROMPTS/#retrieval-subsystem","title":"Retrieval subsystem","text":"<p><code>src/dlgforge/tools/retrieval.py</code> manages a Chroma-backed knowledge store.</p> <p>Key behaviors: - indexes <code>.txt</code>, <code>.md</code>, and <code>.pdf</code> files recursively from the knowledge directory. - chunks content by configurable <code>chunk_size</code> and <code>overlap</code>. - persists index data when <code>persist_dir</code> is configured. - can skip expensive re-indexing via fingerprint/cache checks when unchanged.</p>"},{"location":"RETRIEVAL_AND_PROMPTS/#query-path","title":"Query path","text":"<p>Assistant tool calls can invoke: - <code>vector_db_search(query, k, use_reranker)</code> - optional reranking depending on model settings</p> <p>Returned payload includes rendered evidence snippets and metadata suitable for assistant grounding traces.</p>"},{"location":"RETRIEVAL_AND_PROMPTS/#coverage-aware-sampling","title":"Coverage-aware sampling","text":"<p><code>src/dlgforge/pipeline/sampling.py</code> uses retrieval metadata and coverage ledger memory to: - choose question mode (<code>followup</code>, <code>adjacent</code>, <code>fresh</code>, <code>off_topic</code>, <code>seeded</code>) - sample topic snippets with source-balancing heuristics - avoid recently overused documents/questions - update coverage and seed-topic memories after accepted turns</p>"},{"location":"RETRIEVAL_AND_PROMPTS/#prompt-assets","title":"Prompt assets","text":"<p>Prompt files under <code>src/dlgforge/prompts/</code>: - <code>agents.yaml</code>: role/goal/backstory per logical agent - <code>tasks.yaml</code>: task descriptions and expected JSON output schema - <code>personas.yaml</code>: user/assistant persona pools</p> <p>Prompt assembly: - <code>build_agent_system_prompt(agent_key)</code> - <code>build_task_prompt(task_key, values)</code></p> <p>Templates are rendered with runtime values and enforced as JSON-only contracts in model calls.</p>"},{"location":"RETRIEVAL_AND_PROMPTS/#tool-calling-behavior","title":"Tool-calling behavior","text":"<p>In assistant stage (<code>runner.py</code>): - tool schema always includes KB retrieval. - web search tool is added only when <code>tools.web_search_enabled=true</code>. - tool calls are executed and appended back to chat messages before final assistant JSON is parsed.</p>"},{"location":"RETRIEVAL_AND_PROMPTS/#practical-tuning-knobs","title":"Practical tuning knobs","text":"<ul> <li>retrieval depth: <code>retrieval.default_k</code></li> <li>dedup retries: <code>coverage.question_dedup_retries</code></li> <li>reranker path: <code>models.use_reranker</code> and related model/batch settings</li> <li>language-aware seed topics: <code>run.seed_topics_*</code></li> </ul>"},{"location":"contributing/","title":"Contributing, Issues, and Pull Requests","text":"<p>We welcome bug fixes, reliability improvements, docs updates, test coverage improvements, and developer experience work.</p>"},{"location":"contributing/#contribute-code-or-docs","title":"Contribute code or docs","text":"<ul> <li>Read the project contribution guide: CONTRIBUTING.md</li> <li>Set up locally:   <pre><code>uv venv\nsource .venv/bin/activate\nuv pip install -e .\n</code></pre></li> <li>Run validation before opening a PR:   <pre><code>uv run env PYTHONPATH=src pytest -q\nuv run env PYTHONPATH=src python -m dlgforge --help\nuv run env PYTHONPATH=src python -m dlgforge push --help\n</code></pre></li> </ul>"},{"location":"contributing/#file-an-issue","title":"File an issue","text":"<ul> <li>Bug report: Open bug report</li> <li>Feature request: Open feature request</li> <li>Onboarding/support request: Open onboarding request</li> </ul> <p>For security vulnerabilities, do not open a public issue. Use the private reporting process in the Security policy.</p>"},{"location":"contributing/#open-a-pull-request","title":"Open a pull request","text":"<ul> <li>Use the project PR template: PULL_REQUEST_TEMPLATE.md</li> <li>Include:</li> <li>a clear summary of what changed and why</li> <li>linked issues (if applicable)</li> <li>tests for behavior changes</li> <li>notes on compatibility/operational impact</li> </ul>"},{"location":"contributing/#community-policies","title":"Community policies","text":"<ul> <li>Code of Conduct</li> <li>Security policy</li> <li>Support policy</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Launch DialogForge in a few minutes with a local config and one run command.</p>"},{"location":"getting-started/#1-install-and-set-up","title":"1. Install and set up","text":"<ul> <li>Follow Setup / Installation.</li> <li>Copy environment variables:   <pre><code>cp .env.example .env\n</code></pre></li> </ul>"},{"location":"getting-started/#2-run-generation","title":"2. Run generation","text":"<pre><code>dlgforge run config.yaml\n</code></pre> <p>Equivalent commands: <pre><code>uv run dlgforge run config.yaml\npython -m dlgforge run config.yaml\n</code></pre></p>"},{"location":"getting-started/#3-check-outputs","title":"3. Check outputs","text":"<ul> <li><code>outputs/conversations/*.json</code></li> <li><code>outputs/conversations_sharegpt.jsonl</code></li> <li><code>outputs/turns.jsonl</code></li> <li><code>outputs/run_state/*.json</code></li> <li><code>logs/run.log</code></li> <li><code>logs/llm.log</code></li> <li><code>logs/judge.log</code></li> </ul>"},{"location":"getting-started/#next","title":"Next","text":"<ul> <li>Setup / Installation</li> <li>Configuration</li> <li>CLI / Usage</li> </ul>"},{"location":"setup-installation/","title":"Setup / Installation","text":"<p>Install DialogForge in editable mode and validate the CLI before your first run.</p>"},{"location":"setup-installation/#standard-install","title":"Standard install","text":"<pre><code>uv venv\nsource .venv/bin/activate\nuv pip install -e .\ncp .env.example .env\n</code></pre>"},{"location":"setup-installation/#optional-managed-vllm-extra-linux","title":"Optional managed vLLM extra (Linux)","text":"<pre><code>python -m pip install -e \".[vllm]\"\n</code></pre>"},{"location":"setup-installation/#cli-smoke-checks","title":"CLI smoke checks","text":"<pre><code>uv run env PYTHONPATH=src python -m dlgforge --help\nuv run env PYTHONPATH=src python -m dlgforge push --help\n</code></pre>"},{"location":"setup-installation/#optional-test-baseline","title":"Optional test baseline","text":"<pre><code>uv run env PYTHONPATH=src pytest -q\n</code></pre>"},{"location":"setup-installation/#next","title":"Next","text":"<ul> <li>Getting Started</li> <li>Configuration</li> <li>CLI / Usage</li> </ul>"},{"location":"reference/","title":"API Reference","text":"<p>The API Reference documents DialogForge's public Python surface for operators and contributors.</p> <p>Use the navigation tree to move from package-level entries to concrete modules and symbols. Each page focuses on callable contracts: expected inputs, return values, side effects, and failure conditions.</p>"},{"location":"reference/dlgforge/","title":"<code>dlgforge</code>","text":"<p>Public package entrypoints for DialogForge.</p> <p>This module defines the stable, top-level APIs intended for external callers.</p>"},{"location":"reference/dlgforge/#dlgforge.HFPushOptions","title":"<code>HFPushOptions(repo_id='', repo_type='', source_dir='', export_dir='', include_run_state=False, token=None, commit_message='', prepare_export=True, push=True, clean_remote=False)</code>  <code>dataclass</code>","text":"<p>CLI override options for export/push operations.</p> <p>Parameters:</p> Name Type Description Default <code>repo_id</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>repo_type</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>source_dir</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>export_dir</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>include_run_state</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <code>token</code> <code>Optional[str]</code> <p>Optional[str] value used by this operation.</p> <code>None</code> <code>commit_message</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>prepare_export</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>True</code> <code>push</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>True</code> <code>clean_remote</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>False</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import HFPushOptions\n&gt;&gt;&gt; HFPushOptions(...)\n</code></pre>"},{"location":"reference/dlgforge/#dlgforge.run_push","title":"<code>run_push(config_path, options)</code>","text":"<p>Prepare export artifacts and optionally push them to the Hub.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <code>options</code> <code>HFPushOptions</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import run_push\n&gt;&gt;&gt; run_push(...)\n</code></pre>"},{"location":"reference/dlgforge/#dlgforge.run","title":"<code>run(config_path)</code>","text":"<p>Run synthetic conversation generation from a config file.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import run\n&gt;&gt;&gt; run(...)\n</code></pre>"},{"location":"reference/dlgforge/#dlgforge.run_judge_only","title":"<code>run_judge_only(config_path)</code>","text":"<p>Run judge evaluation on existing conversation artifacts.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import run_judge_only\n&gt;&gt;&gt; run_judge_only(...)\n</code></pre>"},{"location":"reference/dlgforge/#dlgforge.run_seeds_migrate","title":"<code>run_seeds_migrate(config_path, source_file='', dest_file='', overwrite=False)</code>","text":"<p>Run seed-topic migration using project config defaults.</p> <p>The command resolves source and destination paths from CLI overrides first, then from config, then from repository defaults.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <code>source_file</code> <code>str</code> <p>Optional source seed file override (JSON or YAML).</p> <code>''</code> <code>dest_file</code> <code>str</code> <p>Optional destination YAML path override.</p> <code>''</code> <code>overwrite</code> <code>bool</code> <p>Whether to overwrite an existing destination file.</p> <code>False</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Migration summary with source path, destination path,</p> <code>Dict[str, Any]</code> <p>variant list, and topic counts.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If <code>config_path</code> or the resolved source file does not exist.</p> <code>ValueError</code> <p>If the resolved source path points to a directory.</p> <p>Side Effects / I/O:     - Reads config and source files from disk.     - Writes migrated YAML output to disk.     - Writes log entries.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.seed_topics_migration import run_seeds_migrate\n&gt;&gt;&gt; run_seeds_migrate(...)\n</code></pre>"},{"location":"reference/dlgforge/__main__/","title":"<code>dlgforge.__main__</code>","text":"<p>Executable entrypoint for <code>python -m dlgforge</code>.</p> <p>Delegates directly to :func:<code>dlgforge.cli.main</code>.</p>"},{"location":"reference/dlgforge/cli/","title":"<code>dlgforge.cli</code>","text":"<p>CLI parser and command dispatch for DialogForge.</p>"},{"location":"reference/dlgforge/cli/#dlgforge.cli.build_parser","title":"<code>build_parser()</code>","text":"<p>Build the top-level <code>dlgforge</code> argument parser.</p> <p>Returns:</p> Type Description <code>ArgumentParser</code> <p>argparse.ArgumentParser: Parser configured with <code>run</code>, <code>judge</code>, <code>push</code>,</p> <code>ArgumentParser</code> <p>and <code>seeds-migrate</code> subcommands.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.cli import build_parser\n&gt;&gt;&gt; parser = build_parser()\n&gt;&gt;&gt; parser.prog\n'dlgforge'\n</code></pre>"},{"location":"reference/dlgforge/cli/#dlgforge.cli.main","title":"<code>main()</code>","text":"<p>Parse CLI arguments and dispatch to the selected command.</p> <p>The selected subcommand is executed synchronously. Failures are surfaced as <code>SystemExit</code> with a command-scoped error message.</p> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>Always returns <code>None</code> when execution completes.</p> <p>Raises:</p> Type Description <code>SystemExit</code> <p>Raised for command parsing errors or command execution failures.</p> <p>Side Effects / I/O:     - Reads CLI arguments from <code>sys.argv</code>.     - Executes generation, judge, export, or migration workflows depending on       the selected command.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.cli import main\n&gt;&gt;&gt; main()\n</code></pre>"},{"location":"reference/dlgforge/config/","title":"<code>dlgforge.config</code>","text":"<p>Public exports for configuration helpers.</p>"},{"location":"reference/dlgforge/config/#dlgforge.config.build_base_inputs","title":"<code>build_base_inputs(cfg, project_root, config_path)</code>","text":"<p>Build base inputs.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <code>config_path</code> <code>Path</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import build_base_inputs\n&gt;&gt;&gt; build_base_inputs(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.load_config","title":"<code>load_config(config_path)</code>","text":"<p>Load config.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str | Path</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Type Description <code>Tuple[Dict[str, Any], Path, Path]</code> <p>Tuple[Dict[str, Any], Path, Path]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <code>ValueError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import load_config\n&gt;&gt;&gt; load_config(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_batch_size","title":"<code>resolve_batch_size(cfg, fallback=1)</code>","text":"<p>Resolve batch size from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_batch_size\n&gt;&gt;&gt; resolve_batch_size(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_distributed_enabled","title":"<code>resolve_distributed_enabled(cfg)</code>","text":"<p>Resolve distributed enabled from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Boolean indicator describing the evaluated condition.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_distributed_enabled\n&gt;&gt;&gt; resolve_distributed_enabled(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_judge_enabled","title":"<code>resolve_judge_enabled(cfg)</code>","text":"<p>Resolve judge enabled from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Boolean indicator describing the evaluated condition.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_judge_enabled\n&gt;&gt;&gt; resolve_judge_enabled(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_judge_granularity","title":"<code>resolve_judge_granularity(cfg, fallback='turn')</code>","text":"<p>Resolve judge granularity from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>str</code> <p>str value used by this operation.</p> <code>'turn'</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_judge_granularity\n&gt;&gt;&gt; resolve_judge_granularity(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_n_turns","title":"<code>resolve_n_turns(cfg, fallback=1)</code>","text":"<p>Resolve n turns from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_n_turns\n&gt;&gt;&gt; resolve_n_turns(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_output_columns","title":"<code>resolve_output_columns(cfg)</code>","text":"<p>Resolve output columns from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>Dict[str, str]</code> <p>Dict[str, str]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_output_columns\n&gt;&gt;&gt; resolve_output_columns(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_output_dir","title":"<code>resolve_output_dir(cfg, project_root)</code>","text":"<p>Resolve output dir from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_output_dir\n&gt;&gt;&gt; resolve_output_dir(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_retrieval_default_k","title":"<code>resolve_retrieval_default_k(cfg, fallback=4)</code>","text":"<p>Resolve retrieval default k.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>4</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_retrieval_default_k\n&gt;&gt;&gt; resolve_retrieval_default_k(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_seed_topics_variant","title":"<code>resolve_seed_topics_variant(cfg)</code>","text":"<p>Resolve seed topics variant.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_seed_topics_variant\n&gt;&gt;&gt; resolve_seed_topics_variant(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_target_languages","title":"<code>resolve_target_languages(cfg)</code>","text":"<p>Resolve target languages from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List[str]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_target_languages\n&gt;&gt;&gt; resolve_target_languages(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_turn_count_distribution","title":"<code>resolve_turn_count_distribution(cfg, fallback='poisson')</code>","text":"<p>Resolve turn count distribution.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>str</code> <p>str value used by this operation.</p> <code>'poisson'</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_turn_count_distribution\n&gt;&gt;&gt; resolve_turn_count_distribution(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_turn_count_mean","title":"<code>resolve_turn_count_mean(cfg, fallback=0.0)</code>","text":"<p>Resolve turn count mean.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>float</code> <p>float value used by this operation.</p> <code>0.0</code> <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_turn_count_mean\n&gt;&gt;&gt; resolve_turn_count_mean(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_total_samples","title":"<code>resolve_total_samples(cfg, fallback=0)</code>","text":"<p>Resolve total samples from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>0</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_total_samples\n&gt;&gt;&gt; resolve_total_samples(...)\n</code></pre>"},{"location":"reference/dlgforge/config/#dlgforge.config.resolve_turn_range","title":"<code>resolve_turn_range(cfg, fallback=1)</code>","text":"<p>Resolve turn range from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>1</code> <p>Returns:</p> Type Description <code>Tuple[int, int]</code> <p>Tuple[int, int]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_turn_range\n&gt;&gt;&gt; resolve_turn_range(...)\n</code></pre>"},{"location":"reference/dlgforge/config/defaults/","title":"<code>dlgforge.config.defaults</code>","text":"<p>Default configuration schema for DialogForge.</p>"},{"location":"reference/dlgforge/config/loader/","title":"<code>dlgforge.config.loader</code>","text":"<p>Configuration loading and resolver helpers.</p>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.load_config","title":"<code>load_config(config_path)</code>","text":"<p>Load config.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str | Path</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Type Description <code>Tuple[Dict[str, Any], Path, Path]</code> <p>Tuple[Dict[str, Any], Path, Path]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <code>ValueError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import load_config\n&gt;&gt;&gt; load_config(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_output_dir","title":"<code>resolve_output_dir(cfg, project_root)</code>","text":"<p>Resolve output dir from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_output_dir\n&gt;&gt;&gt; resolve_output_dir(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_output_columns","title":"<code>resolve_output_columns(cfg)</code>","text":"<p>Resolve output columns from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>Dict[str, str]</code> <p>Dict[str, str]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_output_columns\n&gt;&gt;&gt; resolve_output_columns(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_question","title":"<code>resolve_question(cfg)</code>","text":"<p>Resolve question from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_question\n&gt;&gt;&gt; resolve_question(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_target_languages","title":"<code>resolve_target_languages(cfg)</code>","text":"<p>Resolve target languages from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List[str]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_target_languages\n&gt;&gt;&gt; resolve_target_languages(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_question_seed","title":"<code>resolve_question_seed(cfg)</code>","text":"<p>Resolve question seed from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_question_seed\n&gt;&gt;&gt; resolve_question_seed(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_run_id","title":"<code>resolve_run_id(cfg)</code>","text":"<p>Resolve run id from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_run_id\n&gt;&gt;&gt; resolve_run_id(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_resume_run_id","title":"<code>resolve_resume_run_id(cfg)</code>","text":"<p>Resolve resume run id.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_resume_run_id\n&gt;&gt;&gt; resolve_resume_run_id(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_n_turns","title":"<code>resolve_n_turns(cfg, fallback=1)</code>","text":"<p>Resolve n turns from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_n_turns\n&gt;&gt;&gt; resolve_n_turns(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_batch_size","title":"<code>resolve_batch_size(cfg, fallback=1)</code>","text":"<p>Resolve batch size from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_batch_size\n&gt;&gt;&gt; resolve_batch_size(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_total_samples","title":"<code>resolve_total_samples(cfg, fallback=0)</code>","text":"<p>Resolve total samples from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>0</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_total_samples\n&gt;&gt;&gt; resolve_total_samples(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_distributed_enabled","title":"<code>resolve_distributed_enabled(cfg)</code>","text":"<p>Resolve distributed enabled from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Boolean indicator describing the evaluated condition.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_distributed_enabled\n&gt;&gt;&gt; resolve_distributed_enabled(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_turn_range","title":"<code>resolve_turn_range(cfg, fallback=1)</code>","text":"<p>Resolve turn range from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>1</code> <p>Returns:</p> Type Description <code>Tuple[int, int]</code> <p>Tuple[int, int]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_turn_range\n&gt;&gt;&gt; resolve_turn_range(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_turn_count_distribution","title":"<code>resolve_turn_count_distribution(cfg, fallback='poisson')</code>","text":"<p>Resolve turn count distribution.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>str</code> <p>str value used by this operation.</p> <code>'poisson'</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_turn_count_distribution\n&gt;&gt;&gt; resolve_turn_count_distribution(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_turn_count_mean","title":"<code>resolve_turn_count_mean(cfg, fallback=0.0)</code>","text":"<p>Resolve turn count mean.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>float</code> <p>float value used by this operation.</p> <code>0.0</code> <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_turn_count_mean\n&gt;&gt;&gt; resolve_turn_count_mean(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_retrieval_default_k","title":"<code>resolve_retrieval_default_k(cfg, fallback=4)</code>","text":"<p>Resolve retrieval default k.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>int</code> <p>int value used by this operation.</p> <code>4</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_retrieval_default_k\n&gt;&gt;&gt; resolve_retrieval_default_k(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_seed_topics_path","title":"<code>resolve_seed_topics_path(cfg)</code>","text":"<p>Resolve seed topics path.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_seed_topics_path\n&gt;&gt;&gt; resolve_seed_topics_path(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_seed_topics_variant","title":"<code>resolve_seed_topics_variant(cfg)</code>","text":"<p>Resolve seed topics variant.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_seed_topics_variant\n&gt;&gt;&gt; resolve_seed_topics_variant(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_seed_topics_probability","title":"<code>resolve_seed_topics_probability(cfg)</code>","text":"<p>Resolve seed topics probability.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_seed_topics_probability\n&gt;&gt;&gt; resolve_seed_topics_probability(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_seed_topics_enabled","title":"<code>resolve_seed_topics_enabled(cfg)</code>","text":"<p>Resolve seed topics enabled.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Boolean indicator describing the evaluated condition.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_seed_topics_enabled\n&gt;&gt;&gt; resolve_seed_topics_enabled(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_judge_mode","title":"<code>resolve_judge_mode(cfg)</code>","text":"<p>Resolve judge mode from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_judge_mode\n&gt;&gt;&gt; resolve_judge_mode(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_judge_enabled","title":"<code>resolve_judge_enabled(cfg)</code>","text":"<p>Resolve judge enabled from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Boolean indicator describing the evaluated condition.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_judge_enabled\n&gt;&gt;&gt; resolve_judge_enabled(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_judge_granularity","title":"<code>resolve_judge_granularity(cfg, fallback='turn')</code>","text":"<p>Resolve judge granularity from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>fallback</code> <code>str</code> <p>str value used by this operation.</p> <code>'turn'</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_judge_granularity\n&gt;&gt;&gt; resolve_judge_granularity(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.resolve_judge_reasons","title":"<code>resolve_judge_reasons(cfg)</code>","text":"<p>Resolve judge reasons from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List[str]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import resolve_judge_reasons\n&gt;&gt;&gt; resolve_judge_reasons(...)\n</code></pre>"},{"location":"reference/dlgforge/config/loader/#dlgforge.config.loader.build_base_inputs","title":"<code>build_base_inputs(cfg, project_root, config_path)</code>","text":"<p>Build base inputs.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <code>config_path</code> <code>Path</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.loader import build_base_inputs\n&gt;&gt;&gt; build_base_inputs(...)\n</code></pre>"},{"location":"reference/dlgforge/config/personas/","title":"<code>dlgforge.config.personas</code>","text":"<p>Persona loading, formatting, and sampling utilities.</p>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.UniformPersonaSampler","title":"<code>UniformPersonaSampler(user_personas, assistant_personas, rng)</code>","text":"<p>Cycle-based sampler for user/assistant personas.</p> <p>The sampler shuffles each persona pool and pops items until exhausted, then reshuffles. This prevents early repetition while keeping distribution near uniform over long runs.</p> <p>Parameters:</p> Name Type Description Default <code>user_personas</code> <code>List[Dict[str, Any]]</code> <p>User persona records.</p> required <code>assistant_personas</code> <code>List[Dict[str, Any]]</code> <p>Assistant persona records.</p> required <code>rng</code> <code>Random</code> <p>Random generator used for deterministic shuffling.</p> required <p>Side Effects / I/O:     - Mutates internal in-memory cycles as samples are drawn.</p> <p>Preconditions / Invariants:     - Persona entries are expected to be mapping-like objects.     - Empty pools are supported and return empty persona text/ids.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import UniformPersonaSampler\n&gt;&gt;&gt; import random\n&gt;&gt;&gt; sampler = UniformPersonaSampler([{\"id\": \"u1\"}], [{\"id\": \"a1\"}], random.Random(7))\n&gt;&gt;&gt; sampler.sample()[2][\"user_id\"]\n'u1'\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.UniformPersonaSampler.sample","title":"<code>sample()</code>","text":"<p>Sample one user persona and one assistant persona.</p> <p>Returns:</p> Type Description <code>str</code> <p>Tuple[str, str, Dict[str, str]]: `(user_text, assistant_text,</p> <code>str</code> <p>metadata)<code>where</code>metadata<code>includes</code>user_id<code>and</code>assistant_id`.</p> <p>Side Effects / I/O:     - Advances in-memory sampling cycles.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import UniformPersonaSampler\n&gt;&gt;&gt; import random\n&gt;&gt;&gt; sampler = UniformPersonaSampler([{\"id\": \"u1\"}], [{\"id\": \"a1\"}], random.Random(0))\n&gt;&gt;&gt; sample = sampler.sample()\n&gt;&gt;&gt; sample[2][\"assistant_id\"]\n'a1'\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.select_personas","title":"<code>select_personas(cfg, project_root, config_path)</code>","text":"<p>Select one user persona and one assistant persona for a conversation.</p> <p>Returns empty values when personas are disabled.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Loaded runtime configuration.</p> required <code>project_root</code> <code>Path</code> <p>Project root used for resolving relative paths.</p> required <code>config_path</code> <code>Path</code> <p>Path to the active config file.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Tuple[str, str, Dict[str, str]]: `(user_text, assistant_text,</p> <code>str</code> <p>metadata)` with selected persona ids in metadata.</p> <p>Side Effects / I/O:     - Reads persona files when persona loading is enabled.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import select_personas\n&gt;&gt;&gt; select_personas(...)\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.resolve_personas_enabled","title":"<code>resolve_personas_enabled(cfg)</code>","text":"<p>Return whether persona sampling is enabled in configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Loaded runtime configuration.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p><code>True</code> when <code>personas.enabled</code> is truthy, otherwise <code>False</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import resolve_personas_enabled\n&gt;&gt;&gt; resolve_personas_enabled({\"personas\": {\"enabled\": True}})\nTrue\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.resolve_personas_path","title":"<code>resolve_personas_path(cfg)</code>","text":"<p>Return the configured personas file path.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Loaded runtime configuration.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Raw value of <code>personas.path</code>, or an empty string when unset.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import resolve_personas_path\n&gt;&gt;&gt; resolve_personas_path({\"personas\": {\"path\": \"data/personas.yaml\"}})\n'data/personas.yaml'\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.resolve_question_seed","title":"<code>resolve_question_seed(cfg)</code>","text":"<p>Return the run-level question seed used for persona RNG initialization.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Loaded runtime configuration.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value of <code>run.question_seed</code>, or an empty string when unset.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import resolve_question_seed\n&gt;&gt;&gt; resolve_question_seed({\"run\": {\"question_seed\": \"seed-1\"}})\n'seed-1'\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.build_persona_rng","title":"<code>build_persona_rng(cfg)</code>","text":"<p>Build a deterministic RNG for persona selection.</p> <p>Uses <code>run.question_seed</code> when present; otherwise falls back to current UTC timestamp for non-deterministic runs.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Loaded runtime configuration.</p> required <p>Returns:</p> Type Description <code>Random</code> <p>random.Random: RNG seeded with a persona-specific seed namespace.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import build_persona_rng\n&gt;&gt;&gt; build_persona_rng(...)\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.build_uniform_persona_sampler","title":"<code>build_uniform_persona_sampler(cfg, project_root, config_path)</code>","text":"<p>Build a <code>UniformPersonaSampler</code> from config and persona assets.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Loaded runtime configuration.</p> required <code>project_root</code> <code>Path</code> <p>Project root used for resolving relative paths.</p> required <code>config_path</code> <code>Path</code> <p>Path to the active config file.</p> required <p>Returns:</p> Name Type Description <code>UniformPersonaSampler</code> <code>UniformPersonaSampler</code> <p>Initialized sampler with user/assistant pools.</p> <p>Side Effects / I/O:     - Reads persona files when enabled.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import build_uniform_persona_sampler\n&gt;&gt;&gt; build_uniform_persona_sampler(...)\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.load_personas","title":"<code>load_personas(cfg, project_root, config_path)</code>","text":"<p>Load user and assistant personas from configuration.</p> <p>Falls back to built-in defaults when the configured file is missing, unreadable, or malformed.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Loaded runtime configuration.</p> required <code>project_root</code> <code>Path</code> <p>Project root used for resolving relative paths.</p> required <code>config_path</code> <code>Path</code> <p>Path to the active config file.</p> required <p>Returns:</p> Type Description <code>Dict[str, List[Dict[str, Any]]]</code> <p>Dict[str, List[Dict[str, Any]]]: Mapping with <code>user</code> and <code>assistant</code></p> <code>Dict[str, List[Dict[str, Any]]]</code> <p>persona lists.</p> <p>Side Effects / I/O:     - Reads YAML from disk when a personas file is configured.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import load_personas\n&gt;&gt;&gt; load_personas(...)\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.default_personas","title":"<code>default_personas()</code>","text":"<p>Return the built-in fallback persona set.</p> <p>Returns:</p> Type Description <code>Dict[str, List[Dict[str, Any]]]</code> <p>Dict[str, List[Dict[str, Any]]]: Mapping with one default user persona</p> <code>Dict[str, List[Dict[str, Any]]]</code> <p>and one default assistant persona.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import default_personas\n&gt;&gt;&gt; sorted(default_personas().keys())\n['assistant', 'user']\n</code></pre>"},{"location":"reference/dlgforge/config/personas/#dlgforge.config.personas.format_persona","title":"<code>format_persona(persona)</code>","text":"<p>Format a persona record into a compact display string.</p> <p>Parameters:</p> Name Type Description Default <code>persona</code> <code>Dict[str, Any]</code> <p>Persona mapping with optional <code>name</code>, <code>traits</code>, and <code>style</code> keys.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Human-readable persona string. Returns an empty string for empty</p> <code>str</code> <p>persona input.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.config.personas import format_persona\n&gt;&gt;&gt; format_persona({'name': 'Tutor', 'traits': ['patient'], 'style': 'Explains step-by-step'})\n'Tutor | patient | Explains step-by-step'\n</code></pre>"},{"location":"reference/dlgforge/distributed/","title":"<code>dlgforge.distributed</code>","text":"<p>Public exports for distributed runtime helpers.</p>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.RunBootstrap","title":"<code>RunBootstrap(config_path, cfg)</code>","text":"<p>Manage distributed bootstrap lifecycle around a generation run.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str | Path</code> <p>Path to a configuration file.</p> required <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.bootstrap import RunBootstrap\n&gt;&gt;&gt; RunBootstrap(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.RunBootstrap.run","title":"<code>run(cfg)</code>  <code>async</code>","text":"<p>Provision runtime dependencies and execute the configured run.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.bootstrap import RunBootstrap\n&gt;&gt;&gt; instance = RunBootstrap(...)\n&gt;&gt;&gt; instance.run(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.AttachProvisioner","title":"<code>AttachProvisioner</code>","text":"<p>Provisioner for attach runtime resources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import AttachProvisioner\n&gt;&gt;&gt; AttachProvisioner(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.AttachProvisioner.start","title":"<code>start(cfg)</code>  <code>async</code>","text":"<p>Start provisioned runtime resources.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[EndpointSpec]</code> <p>List[EndpointSpec]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import AttachProvisioner\n&gt;&gt;&gt; instance = AttachProvisioner(...)\n&gt;&gt;&gt; instance.start(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.AttachProvisioner.stop","title":"<code>stop()</code>  <code>async</code>","text":"<p>Stop provisioned runtime resources.</p> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import AttachProvisioner\n&gt;&gt;&gt; instance = AttachProvisioner(...)\n&gt;&gt;&gt; instance.stop(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.ManagedRayVLLMProvisioner","title":"<code>ManagedRayVLLMProvisioner()</code>","text":"<p>Managed ray VLLM provisioner.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import ManagedRayVLLMProvisioner\n&gt;&gt;&gt; ManagedRayVLLMProvisioner(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.ManagedRayVLLMProvisioner.start","title":"<code>start(cfg)</code>  <code>async</code>","text":"<p>Start provisioned runtime resources.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[EndpointSpec]</code> <p>List[EndpointSpec]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import ManagedRayVLLMProvisioner\n&gt;&gt;&gt; instance = ManagedRayVLLMProvisioner(...)\n&gt;&gt;&gt; instance.start(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.ManagedRayVLLMProvisioner.stop","title":"<code>stop()</code>  <code>async</code>","text":"<p>Stop provisioned runtime resources.</p> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import ManagedRayVLLMProvisioner\n&gt;&gt;&gt; instance = ManagedRayVLLMProvisioner(...)\n&gt;&gt;&gt; instance.stop(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.NoopProvisioner","title":"<code>NoopProvisioner</code>","text":"<p>Provisioner for noop runtime resources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import NoopProvisioner\n&gt;&gt;&gt; NoopProvisioner(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.NoopProvisioner.start","title":"<code>start(cfg)</code>  <code>async</code>","text":"<p>Start provisioned runtime resources.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[EndpointSpec]</code> <p>List[EndpointSpec]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import NoopProvisioner\n&gt;&gt;&gt; instance = NoopProvisioner(...)\n&gt;&gt;&gt; instance.start(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.NoopProvisioner.stop","title":"<code>stop()</code>  <code>async</code>","text":"<p>Stop provisioned runtime resources.</p> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import NoopProvisioner\n&gt;&gt;&gt; instance = NoopProvisioner(...)\n&gt;&gt;&gt; instance.stop(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.VLLMProvisioner","title":"<code>VLLMProvisioner</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Provisioner for vllm runtime resources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import VLLMProvisioner\n&gt;&gt;&gt; VLLMProvisioner(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.VLLMProvisioner.start","title":"<code>start(cfg)</code>  <code>async</code>","text":"<p>Start provisioned runtime resources.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[EndpointSpec]</code> <p>List[EndpointSpec]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import VLLMProvisioner\n&gt;&gt;&gt; instance = VLLMProvisioner(...)\n&gt;&gt;&gt; instance.start(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.VLLMProvisioner.stop","title":"<code>stop()</code>  <code>async</code>","text":"<p>Stop provisioned runtime resources.</p> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import VLLMProvisioner\n&gt;&gt;&gt; instance = VLLMProvisioner(...)\n&gt;&gt;&gt; instance.stop(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.EndpointSpec","title":"<code>EndpointSpec(name, base_url, api_key='', weight=1.0, max_in_flight=64, timeout_s=None)</code>  <code>dataclass</code>","text":"<p>Specification for a routed model endpoint.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>str value used by this operation.</p> required <code>base_url</code> <code>str</code> <p>str value used by this operation.</p> required <code>api_key</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>weight</code> <code>float</code> <p>float value used by this operation.</p> <code>1.0</code> <code>max_in_flight</code> <code>int</code> <p>int value used by this operation.</p> <code>64</code> <code>timeout_s</code> <code>float | None</code> <p>float | None value used by this operation.</p> <code>None</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.types import EndpointSpec\n&gt;&gt;&gt; EndpointSpec(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.EndpointSpec.to_routing_dict","title":"<code>to_routing_dict()</code>","text":"<p>To routing dict.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.types import EndpointSpec\n&gt;&gt;&gt; instance = EndpointSpec(...)\n&gt;&gt;&gt; instance.to_routing_dict(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/#dlgforge.distributed.run_bootstrap","title":"<code>run_bootstrap(config_path, cfg)</code>","text":"<p>Run bootstrap.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str | Path</code> <p>Path to a configuration file.</p> required <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.bootstrap import run_bootstrap\n&gt;&gt;&gt; run_bootstrap(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/bootstrap/","title":"<code>dlgforge.distributed.bootstrap</code>","text":"<p>Distributed bootstrap orchestration for generation runs.</p>"},{"location":"reference/dlgforge/distributed/bootstrap/#dlgforge.distributed.bootstrap.RunBootstrap","title":"<code>RunBootstrap(config_path, cfg)</code>","text":"<p>Manage distributed bootstrap lifecycle around a generation run.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str | Path</code> <p>Path to a configuration file.</p> required <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.bootstrap import RunBootstrap\n&gt;&gt;&gt; RunBootstrap(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/bootstrap/#dlgforge.distributed.bootstrap.RunBootstrap.run","title":"<code>run(cfg)</code>  <code>async</code>","text":"<p>Provision runtime dependencies and execute the configured run.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.bootstrap import RunBootstrap\n&gt;&gt;&gt; instance = RunBootstrap(...)\n&gt;&gt;&gt; instance.run(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/bootstrap/#dlgforge.distributed.bootstrap.run_bootstrap","title":"<code>run_bootstrap(config_path, cfg)</code>","text":"<p>Run bootstrap.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str | Path</code> <p>Path to a configuration file.</p> required <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.bootstrap import run_bootstrap\n&gt;&gt;&gt; run_bootstrap(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/","title":"<code>dlgforge.distributed.provisioning</code>","text":"<p>Provisioners for attached and managed vLLM backends.</p>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.VLLMProvisioner","title":"<code>VLLMProvisioner</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Provisioner for vllm runtime resources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import VLLMProvisioner\n&gt;&gt;&gt; VLLMProvisioner(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.VLLMProvisioner.start","title":"<code>start(cfg)</code>  <code>async</code>","text":"<p>Start provisioned runtime resources.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[EndpointSpec]</code> <p>List[EndpointSpec]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import VLLMProvisioner\n&gt;&gt;&gt; instance = VLLMProvisioner(...)\n&gt;&gt;&gt; instance.start(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.VLLMProvisioner.stop","title":"<code>stop()</code>  <code>async</code>","text":"<p>Stop provisioned runtime resources.</p> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import VLLMProvisioner\n&gt;&gt;&gt; instance = VLLMProvisioner(...)\n&gt;&gt;&gt; instance.stop(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.NoopProvisioner","title":"<code>NoopProvisioner</code>","text":"<p>Provisioner for noop runtime resources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import NoopProvisioner\n&gt;&gt;&gt; NoopProvisioner(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.NoopProvisioner.start","title":"<code>start(cfg)</code>  <code>async</code>","text":"<p>Start provisioned runtime resources.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[EndpointSpec]</code> <p>List[EndpointSpec]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import NoopProvisioner\n&gt;&gt;&gt; instance = NoopProvisioner(...)\n&gt;&gt;&gt; instance.start(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.NoopProvisioner.stop","title":"<code>stop()</code>  <code>async</code>","text":"<p>Stop provisioned runtime resources.</p> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import NoopProvisioner\n&gt;&gt;&gt; instance = NoopProvisioner(...)\n&gt;&gt;&gt; instance.stop(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.AttachProvisioner","title":"<code>AttachProvisioner</code>","text":"<p>Provisioner for attach runtime resources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import AttachProvisioner\n&gt;&gt;&gt; AttachProvisioner(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.AttachProvisioner.start","title":"<code>start(cfg)</code>  <code>async</code>","text":"<p>Start provisioned runtime resources.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[EndpointSpec]</code> <p>List[EndpointSpec]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import AttachProvisioner\n&gt;&gt;&gt; instance = AttachProvisioner(...)\n&gt;&gt;&gt; instance.start(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.AttachProvisioner.stop","title":"<code>stop()</code>  <code>async</code>","text":"<p>Stop provisioned runtime resources.</p> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import AttachProvisioner\n&gt;&gt;&gt; instance = AttachProvisioner(...)\n&gt;&gt;&gt; instance.stop(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.ManagedRayVLLMProvisioner","title":"<code>ManagedRayVLLMProvisioner()</code>","text":"<p>Managed ray VLLM provisioner.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import ManagedRayVLLMProvisioner\n&gt;&gt;&gt; ManagedRayVLLMProvisioner(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.ManagedRayVLLMProvisioner.start","title":"<code>start(cfg)</code>  <code>async</code>","text":"<p>Start provisioned runtime resources.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[EndpointSpec]</code> <p>List[EndpointSpec]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import ManagedRayVLLMProvisioner\n&gt;&gt;&gt; instance = ManagedRayVLLMProvisioner(...)\n&gt;&gt;&gt; instance.start(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/provisioning/#dlgforge.distributed.provisioning.ManagedRayVLLMProvisioner.stop","title":"<code>stop()</code>  <code>async</code>","text":"<p>Stop provisioned runtime resources.</p> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.provisioning import ManagedRayVLLMProvisioner\n&gt;&gt;&gt; instance = ManagedRayVLLMProvisioner(...)\n&gt;&gt;&gt; instance.stop(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/ray_runtime/","title":"<code>dlgforge.distributed.ray_runtime</code>","text":"<p>Ray actor construction and runtime helpers.</p>"},{"location":"reference/dlgforge/distributed/ray_runtime/#dlgforge.distributed.ray_runtime.import_ray","title":"<code>import_ray()</code>","text":"<p>Import ray.</p> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.ray_runtime import import_ray\n&gt;&gt;&gt; import_ray(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/ray_runtime/#dlgforge.distributed.ray_runtime.create_worker_actor","title":"<code>create_worker_actor(role, num_cpus=1.0, num_gpus=0.0)</code>","text":"<p>Create worker actor.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>str value used by this operation.</p> required <code>num_cpus</code> <code>float</code> <p>float value used by this operation.</p> <code>1.0</code> <code>num_gpus</code> <code>float</code> <p>float value used by this operation.</p> <code>0.0</code> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.ray_runtime import create_worker_actor\n&gt;&gt;&gt; create_worker_actor(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/ray_runtime/#dlgforge.distributed.ray_runtime.create_coordinator_actor","title":"<code>create_coordinator_actor(num_cpus=1.0)</code>","text":"<p>Create coordinator actor.</p> <p>Parameters:</p> Name Type Description Default <code>num_cpus</code> <code>float</code> <p>float value used by this operation.</p> <code>1.0</code> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.ray_runtime import create_coordinator_actor\n&gt;&gt;&gt; create_coordinator_actor(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/ray_runtime/#dlgforge.distributed.ray_runtime.create_vllm_server_actor","title":"<code>create_vllm_server_actor(num_cpus=1.0, num_gpus=1.0)</code>","text":"<p>Create vllm server actor.</p> <p>Parameters:</p> Name Type Description Default <code>num_cpus</code> <code>float</code> <p>float value used by this operation.</p> <code>1.0</code> <code>num_gpus</code> <code>float</code> <p>float value used by this operation.</p> <code>1.0</code> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.ray_runtime import create_vllm_server_actor\n&gt;&gt;&gt; create_vllm_server_actor(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/types/","title":"<code>dlgforge.distributed.types</code>","text":"<p>Distributed runtime data types.</p>"},{"location":"reference/dlgforge/distributed/types/#dlgforge.distributed.types.EndpointSpec","title":"<code>EndpointSpec(name, base_url, api_key='', weight=1.0, max_in_flight=64, timeout_s=None)</code>  <code>dataclass</code>","text":"<p>Specification for a routed model endpoint.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>str value used by this operation.</p> required <code>base_url</code> <code>str</code> <p>str value used by this operation.</p> required <code>api_key</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>weight</code> <code>float</code> <p>float value used by this operation.</p> <code>1.0</code> <code>max_in_flight</code> <code>int</code> <p>int value used by this operation.</p> <code>64</code> <code>timeout_s</code> <code>float | None</code> <p>float | None value used by this operation.</p> <code>None</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.types import EndpointSpec\n&gt;&gt;&gt; EndpointSpec(...)\n</code></pre>"},{"location":"reference/dlgforge/distributed/types/#dlgforge.distributed.types.EndpointSpec.to_routing_dict","title":"<code>to_routing_dict()</code>","text":"<p>To routing dict.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.distributed.types import EndpointSpec\n&gt;&gt;&gt; instance = EndpointSpec(...)\n&gt;&gt;&gt; instance.to_routing_dict(...)\n</code></pre>"},{"location":"reference/dlgforge/io/","title":"<code>dlgforge.io</code>","text":"<p>Public exports for output I/O helpers.</p>"},{"location":"reference/dlgforge/io/#dlgforge.io.OutputPaths","title":"<code>OutputPaths(project_root, output_dir)</code>  <code>dataclass</code>","text":"<p>Resolved filesystem paths for all generated output artifacts.</p> <p>Parameters:</p> Name Type Description Default <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <code>output_dir</code> <code>Path</code> <p>Path value used by this operation.</p> required <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import OutputPaths\n&gt;&gt;&gt; OutputPaths(...)\n</code></pre>"},{"location":"reference/dlgforge/io/#dlgforge.io.append_coverage_ledger","title":"<code>append_coverage_ledger(paths, entry)</code>","text":"<p>Append coverage ledger.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>entry</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import append_coverage_ledger\n&gt;&gt;&gt; append_coverage_ledger(...)\n</code></pre>"},{"location":"reference/dlgforge/io/#dlgforge.io.append_sharegpt_judged_record","title":"<code>append_sharegpt_judged_record(paths, conversation_id, timestamp, inputs, turns, messages=None, conversation_judge=None)</code>","text":"<p>Append sharegpt judged record.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>conversation_id</code> <code>str</code> <p>Identifier for a conversation artifact.</p> required <code>timestamp</code> <code>str</code> <p>str value used by this operation.</p> required <code>inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>turns</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <code>messages</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Conversation or message data used during processing.</p> <code>None</code> <code>conversation_judge</code> <code>Optional[Dict[str, Any]]</code> <p>Optional[Dict[str, Any]] value used by this operation.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import append_sharegpt_judged_record\n&gt;&gt;&gt; append_sharegpt_judged_record(...)\n</code></pre>"},{"location":"reference/dlgforge/io/#dlgforge.io.configure_output_columns","title":"<code>configure_output_columns(columns)</code>","text":"<p>Configure output columns.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>Optional[Dict[str, Any]]</code> <p>Optional[Dict[str, Any]] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import configure_output_columns\n&gt;&gt;&gt; configure_output_columns(...)\n</code></pre>"},{"location":"reference/dlgforge/io/#dlgforge.io.ensure_output_layout","title":"<code>ensure_output_layout(paths)</code>","text":"<p>Ensure output layout.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import ensure_output_layout\n&gt;&gt;&gt; ensure_output_layout(...)\n</code></pre>"},{"location":"reference/dlgforge/io/#dlgforge.io.load_coverage_ledger","title":"<code>load_coverage_ledger(paths, max_entries=2000)</code>","text":"<p>Load coverage ledger.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>max_entries</code> <code>int</code> <p>Numeric control value for processing behavior.</p> <code>2000</code> <p>Returns:</p> Type Description <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import load_coverage_ledger\n&gt;&gt;&gt; load_coverage_ledger(...)\n</code></pre>"},{"location":"reference/dlgforge/io/#dlgforge.io.load_run_state","title":"<code>load_run_state(paths, run_id)</code>","text":"<p>Load run state.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>run_id</code> <code>str</code> <p>Identifier for run state tracking.</p> required <p>Returns:</p> Type Description <code>Optional[Dict[str, Any]]</code> <p>Optional[Dict[str, Any]]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import load_run_state\n&gt;&gt;&gt; load_run_state(...)\n</code></pre>"},{"location":"reference/dlgforge/io/#dlgforge.io.save_run_state","title":"<code>save_run_state(paths, run_id, payload)</code>","text":"<p>Save run state.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>run_id</code> <code>str</code> <p>Identifier for run state tracking.</p> required <code>payload</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import save_run_state\n&gt;&gt;&gt; save_run_state(...)\n</code></pre>"},{"location":"reference/dlgforge/io/#dlgforge.io.save_training_sample","title":"<code>save_training_sample(paths, question, inputs, result, turns=None, conversation_history=None, public_history=None, raw_results=None, conversation_id=None)</code>","text":"<p>Save training sample.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>question</code> <code>str</code> <p>str value used by this operation.</p> required <code>inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>result</code> <code>Any</code> <p>Input value for this operation.</p> required <code>turns</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Conversation or message data used during processing.</p> <code>None</code> <code>conversation_history</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Optional[List[Dict[str, Any]]] value used by this operation.</p> <code>None</code> <code>public_history</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Conversation or message data used during processing.</p> <code>None</code> <code>raw_results</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Conversation or message data used during processing.</p> <code>None</code> <code>conversation_id</code> <code>Optional[str]</code> <p>Identifier for a conversation artifact.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import save_training_sample\n&gt;&gt;&gt; save_training_sample(...)\n</code></pre>"},{"location":"reference/dlgforge/io/output/","title":"<code>dlgforge.io.output</code>","text":"<p>Output path definitions and artifact persistence helpers.</p>"},{"location":"reference/dlgforge/io/output/#dlgforge.io.output.OutputPaths","title":"<code>OutputPaths(project_root, output_dir)</code>  <code>dataclass</code>","text":"<p>Resolved filesystem paths for all generated output artifacts.</p> <p>Parameters:</p> Name Type Description Default <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <code>output_dir</code> <code>Path</code> <p>Path value used by this operation.</p> required <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import OutputPaths\n&gt;&gt;&gt; OutputPaths(...)\n</code></pre>"},{"location":"reference/dlgforge/io/output/#dlgforge.io.output.configure_output_columns","title":"<code>configure_output_columns(columns)</code>","text":"<p>Configure output columns.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>Optional[Dict[str, Any]]</code> <p>Optional[Dict[str, Any]] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import configure_output_columns\n&gt;&gt;&gt; configure_output_columns(...)\n</code></pre>"},{"location":"reference/dlgforge/io/output/#dlgforge.io.output.ensure_output_layout","title":"<code>ensure_output_layout(paths)</code>","text":"<p>Ensure output layout.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import ensure_output_layout\n&gt;&gt;&gt; ensure_output_layout(...)\n</code></pre>"},{"location":"reference/dlgforge/io/output/#dlgforge.io.output.load_coverage_ledger","title":"<code>load_coverage_ledger(paths, max_entries=2000)</code>","text":"<p>Load coverage ledger.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>max_entries</code> <code>int</code> <p>Numeric control value for processing behavior.</p> <code>2000</code> <p>Returns:</p> Type Description <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import load_coverage_ledger\n&gt;&gt;&gt; load_coverage_ledger(...)\n</code></pre>"},{"location":"reference/dlgforge/io/output/#dlgforge.io.output.append_coverage_ledger","title":"<code>append_coverage_ledger(paths, entry)</code>","text":"<p>Append coverage ledger.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>entry</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import append_coverage_ledger\n&gt;&gt;&gt; append_coverage_ledger(...)\n</code></pre>"},{"location":"reference/dlgforge/io/output/#dlgforge.io.output.save_run_state","title":"<code>save_run_state(paths, run_id, payload)</code>","text":"<p>Save run state.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>run_id</code> <code>str</code> <p>Identifier for run state tracking.</p> required <code>payload</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import save_run_state\n&gt;&gt;&gt; save_run_state(...)\n</code></pre>"},{"location":"reference/dlgforge/io/output/#dlgforge.io.output.load_run_state","title":"<code>load_run_state(paths, run_id)</code>","text":"<p>Load run state.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>run_id</code> <code>str</code> <p>Identifier for run state tracking.</p> required <p>Returns:</p> Type Description <code>Optional[Dict[str, Any]]</code> <p>Optional[Dict[str, Any]]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import load_run_state\n&gt;&gt;&gt; load_run_state(...)\n</code></pre>"},{"location":"reference/dlgforge/io/output/#dlgforge.io.output.save_training_sample","title":"<code>save_training_sample(paths, question, inputs, result, turns=None, conversation_history=None, public_history=None, raw_results=None, conversation_id=None)</code>","text":"<p>Save training sample.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>question</code> <code>str</code> <p>str value used by this operation.</p> required <code>inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>result</code> <code>Any</code> <p>Input value for this operation.</p> required <code>turns</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Conversation or message data used during processing.</p> <code>None</code> <code>conversation_history</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Optional[List[Dict[str, Any]]] value used by this operation.</p> <code>None</code> <code>public_history</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Conversation or message data used during processing.</p> <code>None</code> <code>raw_results</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Conversation or message data used during processing.</p> <code>None</code> <code>conversation_id</code> <code>Optional[str]</code> <p>Identifier for a conversation artifact.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import save_training_sample\n&gt;&gt;&gt; save_training_sample(...)\n</code></pre>"},{"location":"reference/dlgforge/io/output/#dlgforge.io.output.append_sharegpt_judged_record","title":"<code>append_sharegpt_judged_record(paths, conversation_id, timestamp, inputs, turns, messages=None, conversation_judge=None)</code>","text":"<p>Append sharegpt judged record.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>conversation_id</code> <code>str</code> <p>Identifier for a conversation artifact.</p> required <code>timestamp</code> <code>str</code> <p>str value used by this operation.</p> required <code>inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>turns</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <code>messages</code> <code>Optional[List[Dict[str, Any]]]</code> <p>Conversation or message data used during processing.</p> <code>None</code> <code>conversation_judge</code> <code>Optional[Dict[str, Any]]</code> <p>Optional[Dict[str, Any]] value used by this operation.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.io.output import append_sharegpt_judged_record\n&gt;&gt;&gt; append_sharegpt_judged_record(...)\n</code></pre>"},{"location":"reference/dlgforge/llm/","title":"<code>dlgforge.llm</code>","text":"<p>Public exports for LLM settings and client helpers.</p>"},{"location":"reference/dlgforge/llm/#dlgforge.llm.ChatResult","title":"<code>ChatResult(content, tool_calls, raw)</code>  <code>dataclass</code>","text":"<p>Normalized chat completion output.</p>"},{"location":"reference/dlgforge/llm/#dlgforge.llm.OpenAIModelClient","title":"<code>OpenAIModelClient()</code>","text":"<p>OpenAI-compatible public client, backed internally by LiteLLM.</p>"},{"location":"reference/dlgforge/llm/#dlgforge.llm.missing_models","title":"<code>missing_models(cfg)</code>","text":"<p>Return missing models.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List[str]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.llm.settings import missing_models\n&gt;&gt;&gt; missing_models(...)\n</code></pre>"},{"location":"reference/dlgforge/llm/#dlgforge.llm.required_agents","title":"<code>required_agents(cfg)</code>","text":"<p>Return required agents.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List[str]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.llm.settings import required_agents\n&gt;&gt;&gt; required_agents(...)\n</code></pre>"},{"location":"reference/dlgforge/llm/#dlgforge.llm.resolve_agent_used_name","title":"<code>resolve_agent_used_name(cfg, agent_key)</code>","text":"<p>Resolve agent used name.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>agent_key</code> <code>str</code> <p>str value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.llm.settings import resolve_agent_used_name\n&gt;&gt;&gt; resolve_agent_used_name(...)\n</code></pre>"},{"location":"reference/dlgforge/llm/#dlgforge.llm.resolve_llm_settings","title":"<code>resolve_llm_settings(cfg, agent_key)</code>","text":"<p>Resolve llm settings from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>agent_key</code> <code>str</code> <p>str value used by this operation.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.llm.settings import resolve_llm_settings\n&gt;&gt;&gt; resolve_llm_settings(...)\n</code></pre>"},{"location":"reference/dlgforge/llm/client/","title":"<code>dlgforge.llm.client</code>","text":"<p>LiteLLM-backed chat client wrappers with routing support.</p>"},{"location":"reference/dlgforge/llm/client/#dlgforge.llm.client.ChatResult","title":"<code>ChatResult(content, tool_calls, raw)</code>  <code>dataclass</code>","text":"<p>Normalized chat completion output.</p>"},{"location":"reference/dlgforge/llm/client/#dlgforge.llm.client.OpenAIModelClient","title":"<code>OpenAIModelClient()</code>","text":"<p>OpenAI-compatible public client, backed internally by LiteLLM.</p>"},{"location":"reference/dlgforge/llm/settings/","title":"<code>dlgforge.llm.settings</code>","text":"<p>LLM settings resolution and agent model requirements.</p>"},{"location":"reference/dlgforge/llm/settings/#dlgforge.llm.settings.resolve_llm_settings","title":"<code>resolve_llm_settings(cfg, agent_key)</code>","text":"<p>Resolve llm settings from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>agent_key</code> <code>str</code> <p>str value used by this operation.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.llm.settings import resolve_llm_settings\n&gt;&gt;&gt; resolve_llm_settings(...)\n</code></pre>"},{"location":"reference/dlgforge/llm/settings/#dlgforge.llm.settings.resolve_agent_used_name","title":"<code>resolve_agent_used_name(cfg, agent_key)</code>","text":"<p>Resolve agent used name.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>agent_key</code> <code>str</code> <p>str value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.llm.settings import resolve_agent_used_name\n&gt;&gt;&gt; resolve_agent_used_name(...)\n</code></pre>"},{"location":"reference/dlgforge/llm/settings/#dlgforge.llm.settings.required_agents","title":"<code>required_agents(cfg)</code>","text":"<p>Return required agents.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List[str]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.llm.settings import required_agents\n&gt;&gt;&gt; required_agents(...)\n</code></pre>"},{"location":"reference/dlgforge/llm/settings/#dlgforge.llm.settings.missing_models","title":"<code>missing_models(cfg)</code>","text":"<p>Return missing models.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List[str]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.llm.settings import missing_models\n&gt;&gt;&gt; missing_models(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/","title":"<code>dlgforge.pipeline</code>","text":"<p>Public exports for pipeline entrypoints.</p>"},{"location":"reference/dlgforge/pipeline/#dlgforge.pipeline.HFPushOptions","title":"<code>HFPushOptions(repo_id='', repo_type='', source_dir='', export_dir='', include_run_state=False, token=None, commit_message='', prepare_export=True, push=True, clean_remote=False)</code>  <code>dataclass</code>","text":"<p>CLI override options for export/push operations.</p> <p>Parameters:</p> Name Type Description Default <code>repo_id</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>repo_type</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>source_dir</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>export_dir</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>include_run_state</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <code>token</code> <code>Optional[str]</code> <p>Optional[str] value used by this operation.</p> <code>None</code> <code>commit_message</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>prepare_export</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>True</code> <code>push</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>True</code> <code>clean_remote</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>False</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import HFPushOptions\n&gt;&gt;&gt; HFPushOptions(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/#dlgforge.pipeline.run_push","title":"<code>run_push(config_path, options)</code>","text":"<p>Prepare export artifacts and optionally push them to the Hub.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <code>options</code> <code>HFPushOptions</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import run_push\n&gt;&gt;&gt; run_push(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/#dlgforge.pipeline.run","title":"<code>run(config_path)</code>","text":"<p>Run synthetic conversation generation from a config file.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import run\n&gt;&gt;&gt; run(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/#dlgforge.pipeline.run_judge_only","title":"<code>run_judge_only(config_path)</code>","text":"<p>Run judge evaluation on existing conversation artifacts.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import run_judge_only\n&gt;&gt;&gt; run_judge_only(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/#dlgforge.pipeline.run_seeds_migrate","title":"<code>run_seeds_migrate(config_path, source_file='', dest_file='', overwrite=False)</code>","text":"<p>Run seed-topic migration using project config defaults.</p> <p>The command resolves source and destination paths from CLI overrides first, then from config, then from repository defaults.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <code>source_file</code> <code>str</code> <p>Optional source seed file override (JSON or YAML).</p> <code>''</code> <code>dest_file</code> <code>str</code> <p>Optional destination YAML path override.</p> <code>''</code> <code>overwrite</code> <code>bool</code> <p>Whether to overwrite an existing destination file.</p> <code>False</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Migration summary with source path, destination path,</p> <code>Dict[str, Any]</code> <p>variant list, and topic counts.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If <code>config_path</code> or the resolved source file does not exist.</p> <code>ValueError</code> <p>If the resolved source path points to a directory.</p> <p>Side Effects / I/O:     - Reads config and source files from disk.     - Writes migrated YAML output to disk.     - Writes log entries.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.seed_topics_migration import run_seeds_migrate\n&gt;&gt;&gt; run_seeds_migrate(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/dedup/","title":"<code>dlgforge.pipeline.dedup</code>","text":"<p>Question normalization and dedup registry helpers.</p>"},{"location":"reference/dlgforge/pipeline/dedup/#dlgforge.pipeline.dedup.RunQuestionRegistry","title":"<code>RunQuestionRegistry(initial_questions=None)</code>","text":"<p>Helper for runquestionregistry.</p> <p>Parameters:</p> Name Type Description Default <code>initial_questions</code> <code>Iterable[str] | None</code> <p>Iterable[str] | None value used by this operation.</p> <code>None</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.dedup import RunQuestionRegistry\n&gt;&gt;&gt; RunQuestionRegistry(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/dedup/#dlgforge.pipeline.dedup.RunQuestionRegistry.filter_and_commit","title":"<code>filter_and_commit(candidates)</code>  <code>async</code>","text":"<p>Filter and commit.</p> <p>Parameters:</p> Name Type Description Default <code>candidates</code> <code>Sequence[Tuple[int, str]]</code> <p>Sequence[Tuple[int, str]] value used by this operation.</p> required <p>Returns:</p> Type Description <code>Tuple[Set[int], Set[int]]</code> <p>Tuple[Set[int], Set[int]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.dedup import RunQuestionRegistry\n&gt;&gt;&gt; instance = RunQuestionRegistry(...)\n&gt;&gt;&gt; instance.filter_and_commit(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/dedup/#dlgforge.pipeline.dedup.normalize_question","title":"<code>normalize_question(text)</code>","text":"<p>Normalize question.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.dedup import normalize_question\n&gt;&gt;&gt; normalize_question(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/hf_push/","title":"<code>dlgforge.pipeline.hf_push</code>","text":"<p>Hugging Face export preparation and push helpers.</p>"},{"location":"reference/dlgforge/pipeline/hf_push/#dlgforge.pipeline.hf_push.HFPushSettings","title":"<code>HFPushSettings(enabled, auto_push_on_run, repo_id, repo_type, export_dir, include_run_state, private, commit_message, source_file, clean_remote, generate_stats, stats_file, generate_plots, plots_dir, output_columns)</code>  <code>dataclass</code>","text":"<p>Resolved Hugging Face export/push settings.</p> <p>Parameters:</p> Name Type Description Default <code>enabled</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> required <code>auto_push_on_run</code> <code>bool</code> <p>bool value used by this operation.</p> required <code>repo_id</code> <code>str</code> <p>str value used by this operation.</p> required <code>repo_type</code> <code>str</code> <p>str value used by this operation.</p> required <code>export_dir</code> <code>Path</code> <p>Path value used by this operation.</p> required <code>include_run_state</code> <code>bool</code> <p>bool value used by this operation.</p> required <code>private</code> <code>bool</code> <p>bool value used by this operation.</p> required <code>commit_message</code> <code>str</code> <p>str value used by this operation.</p> required <code>source_file</code> <code>str</code> <p>Filesystem path used by this operation.</p> required <code>clean_remote</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> required <code>generate_stats</code> <code>bool</code> <p>bool value used by this operation.</p> required <code>stats_file</code> <code>str</code> <p>str value used by this operation.</p> required <code>generate_plots</code> <code>bool</code> <p>bool value used by this operation.</p> required <code>plots_dir</code> <code>str</code> <p>str value used by this operation.</p> required <code>output_columns</code> <code>Dict[str, str]</code> <p>Dict[str, str] value used by this operation.</p> required <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import HFPushSettings\n&gt;&gt;&gt; HFPushSettings(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/hf_push/#dlgforge.pipeline.hf_push.HFPushOptions","title":"<code>HFPushOptions(repo_id='', repo_type='', source_dir='', export_dir='', include_run_state=False, token=None, commit_message='', prepare_export=True, push=True, clean_remote=False)</code>  <code>dataclass</code>","text":"<p>CLI override options for export/push operations.</p> <p>Parameters:</p> Name Type Description Default <code>repo_id</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>repo_type</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>source_dir</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>export_dir</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>include_run_state</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <code>token</code> <code>Optional[str]</code> <p>Optional[str] value used by this operation.</p> <code>None</code> <code>commit_message</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>prepare_export</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>True</code> <code>push</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>True</code> <code>clean_remote</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>False</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import HFPushOptions\n&gt;&gt;&gt; HFPushOptions(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/hf_push/#dlgforge.pipeline.hf_push.run_push","title":"<code>run_push(config_path, options)</code>","text":"<p>Prepare export artifacts and optionally push them to the Hub.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <code>options</code> <code>HFPushOptions</code> <p>Configuration mapping that controls runtime behavior.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import run_push\n&gt;&gt;&gt; run_push(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/hf_push/#dlgforge.pipeline.hf_push.maybe_auto_push_after_run","title":"<code>maybe_auto_push_after_run(cfg, output_paths)</code>","text":"<p>Conditionally execute auto push after run.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>output_paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import maybe_auto_push_after_run\n&gt;&gt;&gt; maybe_auto_push_after_run(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/hf_push/#dlgforge.pipeline.hf_push.resolve_hf_push_settings","title":"<code>resolve_hf_push_settings(cfg, project_root)</code>","text":"<p>Resolve hf push settings.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <p>Returns:</p> Name Type Description <code>HFPushSettings</code> <code>HFPushSettings</code> <p>Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import resolve_hf_push_settings\n&gt;&gt;&gt; resolve_hf_push_settings(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/hf_push/#dlgforge.pipeline.hf_push.prepare_export","title":"<code>prepare_export(source_dir, export_dir, source_file, include_run_state, repo_id, generate_stats=False, stats_file='dataset_stats.json', generate_plots=False, plots_dir='plots', output_columns=None)</code>","text":"<p>Prepare export.</p> <p>Parameters:</p> Name Type Description Default <code>source_dir</code> <code>Path</code> <p>Path value used by this operation.</p> required <code>export_dir</code> <code>Path</code> <p>Path value used by this operation.</p> required <code>source_file</code> <code>str</code> <p>Filesystem path used by this operation.</p> required <code>include_run_state</code> <code>bool</code> <p>bool value used by this operation.</p> required <code>repo_id</code> <code>str | None</code> <p>str | None value used by this operation.</p> required <code>generate_stats</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <code>stats_file</code> <code>str</code> <p>str value used by this operation.</p> <code>'dataset_stats.json'</code> <code>generate_plots</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <code>plots_dir</code> <code>str</code> <p>str value used by this operation.</p> <code>'plots'</code> <code>output_columns</code> <code>Optional[Dict[str, str]]</code> <p>Optional[Dict[str, str]] value used by this operation.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import prepare_export\n&gt;&gt;&gt; prepare_export(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/hf_push/#dlgforge.pipeline.hf_push.push_to_hub","title":"<code>push_to_hub(export_dir, repo_id, repo_type, token, commit_message, private=True, clean_remote=False)</code>","text":"<p>Push to hub.</p> <p>Parameters:</p> Name Type Description Default <code>export_dir</code> <code>Path</code> <p>Path value used by this operation.</p> required <code>repo_id</code> <code>str</code> <p>str value used by this operation.</p> required <code>repo_type</code> <code>str</code> <p>str value used by this operation.</p> required <code>token</code> <code>Optional[str]</code> <p>Optional[str] value used by this operation.</p> required <code>commit_message</code> <code>str</code> <p>str value used by this operation.</p> required <code>private</code> <code>bool</code> <p>bool value used by this operation.</p> <code>True</code> <code>clean_remote</code> <code>bool</code> <p>Boolean flag that controls optional behavior.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Raised when validation or runtime requirements are not met.</p> <code>ImportError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.hf_push import push_to_hub\n&gt;&gt;&gt; push_to_hub(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/history/","title":"<code>dlgforge.pipeline.history</code>","text":"<p>Conversation history formatting helpers.</p>"},{"location":"reference/dlgforge/pipeline/history/#dlgforge.pipeline.history.format_history","title":"<code>format_history(history)</code>","text":"<p>Format history.</p> <p>Parameters:</p> Name Type Description Default <code>history</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.history import format_history\n&gt;&gt;&gt; format_history(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/history/#dlgforge.pipeline.history.build_conversation_history","title":"<code>build_conversation_history(turns)</code>","text":"<p>Build conversation history.</p> <p>Parameters:</p> Name Type Description Default <code>turns</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <p>Returns:</p> Type Description <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.history import build_conversation_history\n&gt;&gt;&gt; build_conversation_history(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/history/#dlgforge.pipeline.history.build_public_history","title":"<code>build_public_history(turns)</code>","text":"<p>Build public history.</p> <p>Parameters:</p> Name Type Description Default <code>turns</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <p>Returns:</p> Type Description <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.history import build_public_history\n&gt;&gt;&gt; build_public_history(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/history/#dlgforge.pipeline.history.messages_up_to_turn","title":"<code>messages_up_to_turn(messages, turn_index)</code>","text":"<p>Messages up to turn.</p> <p>Parameters:</p> Name Type Description Default <code>messages</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <code>turn_index</code> <code>int | None</code> <p>Numeric control value for processing behavior.</p> required <p>Returns:</p> Type Description <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.history import messages_up_to_turn\n&gt;&gt;&gt; messages_up_to_turn(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/prompts/","title":"<code>dlgforge.pipeline.prompts</code>","text":"<p>Prompt asset loading and template rendering helpers.</p>"},{"location":"reference/dlgforge/pipeline/prompts/#dlgforge.pipeline.prompts.load_agents_config","title":"<code>load_agents_config()</code>  <code>cached</code>","text":"<p>Load agents config.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.prompts import load_agents_config\n&gt;&gt;&gt; load_agents_config(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/prompts/#dlgforge.pipeline.prompts.load_tasks_config","title":"<code>load_tasks_config()</code>  <code>cached</code>","text":"<p>Load tasks config.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.prompts import load_tasks_config\n&gt;&gt;&gt; load_tasks_config(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/prompts/#dlgforge.pipeline.prompts.build_agent_system_prompt","title":"<code>build_agent_system_prompt(agent_key)</code>","text":"<p>Build agent system prompt.</p> <p>Parameters:</p> Name Type Description Default <code>agent_key</code> <code>str</code> <p>str value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.prompts import build_agent_system_prompt\n&gt;&gt;&gt; build_agent_system_prompt(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/prompts/#dlgforge.pipeline.prompts.build_task_prompt","title":"<code>build_task_prompt(task_key, values)</code>","text":"<p>Build task prompt.</p> <p>Parameters:</p> Name Type Description Default <code>task_key</code> <code>str</code> <p>str value used by this operation.</p> required <code>values</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.prompts import build_task_prompt\n&gt;&gt;&gt; build_task_prompt(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/runner/","title":"<code>dlgforge.pipeline.runner</code>","text":"<p>Main generation and judge orchestration flows.</p>"},{"location":"reference/dlgforge/pipeline/runner/#dlgforge.pipeline.runner.run","title":"<code>run(config_path)</code>","text":"<p>Run synthetic conversation generation from a config file.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import run\n&gt;&gt;&gt; run(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/runner/#dlgforge.pipeline.runner.run_judge_only","title":"<code>run_judge_only(config_path)</code>","text":"<p>Run judge evaluation on existing conversation artifacts.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Raised when validation or runtime requirements are not met.</p> <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import run_judge_only\n&gt;&gt;&gt; run_judge_only(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/runner/#dlgforge.pipeline.runner.run_multi_turn","title":"<code>run_multi_turn(cfg, output_paths, base_inputs, min_turns, max_turns, turn_count_distribution, turn_count_mean)</code>","text":"<p>Run multi turn.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>output_paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>min_turns</code> <code>int</code> <p>int value used by this operation.</p> required <code>max_turns</code> <code>int</code> <p>int value used by this operation.</p> required <code>turn_count_distribution</code> <code>str</code> <p>str value used by this operation.</p> required <code>turn_count_mean</code> <code>float</code> <p>float value used by this operation.</p> required <p>Returns:</p> Type Description <code>Tuple[List[Dict[str, Any]], List[Dict[str, Any]], Dict[str, Any]]</code> <p>Tuple[List[Dict[str, Any]], List[Dict[str, Any]], Dict[str, Any]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import run_multi_turn\n&gt;&gt;&gt; run_multi_turn(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/runner/#dlgforge.pipeline.runner.run_multi_turn_batched_async","title":"<code>run_multi_turn_batched_async(cfg, output_paths, base_inputs, max_turns, batch_size, min_turns=1, turn_count_distribution='poisson', turn_count_mean=0.0, conversation_inputs_by_index=None)</code>  <code>async</code>","text":"<p>Run multi turn batched async.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>output_paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>max_turns</code> <code>int</code> <p>int value used by this operation.</p> required <code>batch_size</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>min_turns</code> <code>int</code> <p>int value used by this operation.</p> <code>1</code> <code>turn_count_distribution</code> <code>str</code> <p>str value used by this operation.</p> <code>'poisson'</code> <code>turn_count_mean</code> <code>float</code> <p>float value used by this operation.</p> <code>0.0</code> <code>conversation_inputs_by_index</code> <code>List[Dict[str, Any]] | None</code> <p>List[Dict[str, Any]] | None value used by this operation.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tuple[Dict[str, Any], List[Dict[str, Any]]]</code> <p>Tuple[Dict[str, Any], List[Dict[str, Any]]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import run_multi_turn_batched_async\n&gt;&gt;&gt; run_multi_turn_batched_async(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/runner/#dlgforge.pipeline.runner.persist_training_sample","title":"<code>persist_training_sample(output_paths, inputs, result, turns, raw_results)</code>","text":"<p>Persist training sample.</p> <p>Parameters:</p> Name Type Description Default <code>output_paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>result</code> <code>Any</code> <p>Input value for this operation.</p> required <code>turns</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <code>raw_results</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import persist_training_sample\n&gt;&gt;&gt; persist_training_sample(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/runner/#dlgforge.pipeline.runner.persist_batched_training_samples","title":"<code>persist_batched_training_samples(output_paths, base_inputs, conversations)</code>","text":"<p>Persist batched training samples.</p> <p>Parameters:</p> Name Type Description Default <code>output_paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>conversations</code> <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.runner import persist_batched_training_samples\n&gt;&gt;&gt; persist_batched_training_samples(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/","title":"<code>dlgforge.pipeline.sampling</code>","text":"<p>Question sampling, coverage memory, and seed-topic helpers.</p>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.build_question_inputs","title":"<code>build_question_inputs(base_inputs, turn_index, n_turns, public_history, used_topic_ids, recent_ledger_questions, doc_usage, doc_chunk_counts, doc_recent_questions, avoid_sources, forced_mode, used_seed_hashes, seed_topic_usage)</code>","text":"<p>Build question inputs.</p> <p>Parameters:</p> Name Type Description Default <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>turn_index</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>n_turns</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>public_history</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <code>used_topic_ids</code> <code>set</code> <p>set value used by this operation.</p> required <code>recent_ledger_questions</code> <code>List[str]</code> <p>List[str] value used by this operation.</p> required <code>doc_usage</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <code>doc_chunk_counts</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <code>doc_recent_questions</code> <code>Dict[str, List[str]]</code> <p>Dict[str, List[str]] value used by this operation.</p> required <code>avoid_sources</code> <code>set[str]</code> <p>set[str] value used by this operation.</p> required <code>forced_mode</code> <code>str</code> <p>str value used by this operation.</p> required <code>used_seed_hashes</code> <code>set[str]</code> <p>set[str] value used by this operation.</p> required <code>seed_topic_usage</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import build_question_inputs\n&gt;&gt;&gt; build_question_inputs(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.build_rng","title":"<code>build_rng(base_inputs, turn_index)</code>","text":"<p>Build rng.</p> <p>Parameters:</p> Name Type Description Default <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>turn_index</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <p>Returns:</p> Type Description <code>Random</code> <p>random.Random: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import build_rng\n&gt;&gt;&gt; build_rng(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.select_question_mode","title":"<code>select_question_mode(turn_index, n_turns, has_assistant, rng, seed_query)</code>","text":"<p>Select question mode.</p> <p>Parameters:</p> Name Type Description Default <code>turn_index</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>n_turns</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>has_assistant</code> <code>bool</code> <p>bool value used by this operation.</p> required <code>rng</code> <code>Random</code> <p>random.Random value used by this operation.</p> required <code>seed_query</code> <code>str</code> <p>str value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import select_question_mode\n&gt;&gt;&gt; select_question_mode(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.sample_topic_snippets","title":"<code>sample_topic_snippets(mode, seed_query, last_assistant_message, used_topic_ids, doc_usage, doc_chunk_counts, doc_recent_questions, avoid_sources, rng)</code>","text":"<p>Sample topic snippets.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>str value used by this operation.</p> required <code>seed_query</code> <code>str</code> <p>str value used by this operation.</p> required <code>last_assistant_message</code> <code>str</code> <p>str value used by this operation.</p> required <code>used_topic_ids</code> <code>set</code> <p>set value used by this operation.</p> required <code>doc_usage</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <code>doc_chunk_counts</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <code>doc_recent_questions</code> <code>Dict[str, List[str]]</code> <p>Dict[str, List[str]] value used by this operation.</p> required <code>avoid_sources</code> <code>set[str]</code> <p>set[str] value used by this operation.</p> required <code>rng</code> <code>Random</code> <p>random.Random value used by this operation.</p> required <p>Returns:</p> Type Description <code>List[Dict[str, str]]</code> <p>List[Dict[str, str]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import sample_topic_snippets\n&gt;&gt;&gt; sample_topic_snippets(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.format_source_descriptor","title":"<code>format_source_descriptor(metadata)</code>","text":"<p>Format source descriptor.</p> <p>Parameters:</p> Name Type Description Default <code>metadata</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import format_source_descriptor\n&gt;&gt;&gt; format_source_descriptor(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.build_doc_usage","title":"<code>build_doc_usage(ledger_entries)</code>","text":"<p>Build doc usage.</p> <p>Parameters:</p> Name Type Description Default <code>ledger_entries</code> <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]] value used by this operation.</p> required <p>Returns:</p> Type Description <code>Dict[str, int]</code> <p>Dict[str, int]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import build_doc_usage\n&gt;&gt;&gt; build_doc_usage(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.build_doc_chunk_counts","title":"<code>build_doc_chunk_counts()</code>","text":"<p>Build doc chunk counts.</p> <p>Returns:</p> Type Description <code>Dict[str, int]</code> <p>Dict[str, int]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import build_doc_chunk_counts\n&gt;&gt;&gt; build_doc_chunk_counts(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.build_doc_question_hashes","title":"<code>build_doc_question_hashes(ledger_entries)</code>","text":"<p>Build doc question hashes.</p> <p>Parameters:</p> Name Type Description Default <code>ledger_entries</code> <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]] value used by this operation.</p> required <p>Returns:</p> Type Description <code>Dict[str, set[str]]</code> <p>Dict[str, set[str]]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import build_doc_question_hashes\n&gt;&gt;&gt; build_doc_question_hashes(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.build_doc_recent_questions","title":"<code>build_doc_recent_questions(ledger_entries, max_per_doc=8)</code>","text":"<p>Build doc recent questions.</p> <p>Parameters:</p> Name Type Description Default <code>ledger_entries</code> <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]] value used by this operation.</p> required <code>max_per_doc</code> <code>int</code> <p>int value used by this operation.</p> <code>8</code> <p>Returns:</p> Type Description <code>Dict[str, List[str]]</code> <p>Dict[str, List[str]]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import build_doc_recent_questions\n&gt;&gt;&gt; build_doc_recent_questions(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.select_doc_pool","title":"<code>select_doc_pool(doc_usage, doc_chunk_counts, all_sources, rng)</code>","text":"<p>Select doc pool.</p> <p>Parameters:</p> Name Type Description Default <code>doc_usage</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <code>doc_chunk_counts</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <code>all_sources</code> <code>List[str]</code> <p>List[str] value used by this operation.</p> required <code>rng</code> <code>Random</code> <p>random.Random value used by this operation.</p> required <p>Returns:</p> Type Description <code>set[str]</code> <p>set[str]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import select_doc_pool\n&gt;&gt;&gt; select_doc_pool(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.coverage_ratio","title":"<code>coverage_ratio(source, doc_usage, doc_chunk_counts)</code>","text":"<p>Coverage ratio.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>str</code> <p>Filesystem path used by this operation.</p> required <code>doc_usage</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <code>doc_chunk_counts</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import coverage_ratio\n&gt;&gt;&gt; coverage_ratio(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.filter_results_by_sources","title":"<code>filter_results_by_sources(results, preferred_sources, allow_fallback)</code>","text":"<p>Filter results by sources.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>List[tuple]</code> <p>List[tuple] value used by this operation.</p> required <code>preferred_sources</code> <code>set[str]</code> <p>set[str] value used by this operation.</p> required <code>allow_fallback</code> <code>bool</code> <p>bool value used by this operation.</p> required <p>Returns:</p> Type Description <code>List[tuple]</code> <p>List[tuple]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import filter_results_by_sources\n&gt;&gt;&gt; filter_results_by_sources(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.clamp_float","title":"<code>clamp_float(raw, default)</code>","text":"<p>Clamp float.</p> <p>Parameters:</p> Name Type Description Default <code>raw</code> <code>str</code> <p>str value used by this operation.</p> required <code>default</code> <code>float</code> <p>float value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import clamp_float\n&gt;&gt;&gt; clamp_float(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.update_coverage_ledger","title":"<code>update_coverage_ledger(paths, qa_output, question_inputs, used_topic_ids, used_question_hashes)</code>","text":"<p>Update coverage ledger.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>qa_output</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>question_inputs</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>used_topic_ids</code> <code>set</code> <p>set value used by this operation.</p> required <code>used_question_hashes</code> <code>set</code> <p>set value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import update_coverage_ledger\n&gt;&gt;&gt; update_coverage_ledger(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.is_duplicate_question","title":"<code>is_duplicate_question(qa_output, question_inputs, doc_question_hashes)</code>","text":"<p>Check whether duplicate question.</p> <p>Parameters:</p> Name Type Description Default <code>qa_output</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>question_inputs</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>doc_question_hashes</code> <code>Dict[str, set[str]]</code> <p>Dict[str, set[str]] value used by this operation.</p> required <p>Returns:</p> Type Description <code>tuple[bool, str]</code> <p>tuple[bool, str]: Boolean indicator describing the evaluated condition.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import is_duplicate_question\n&gt;&gt;&gt; is_duplicate_question(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.update_doc_question_memory","title":"<code>update_doc_question_memory(qa_output, question_inputs, doc_question_hashes, doc_recent_questions)</code>","text":"<p>Update doc question memory.</p> <p>Parameters:</p> Name Type Description Default <code>qa_output</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>question_inputs</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>doc_question_hashes</code> <code>Dict[str, set[str]]</code> <p>Dict[str, set[str]] value used by this operation.</p> required <code>doc_recent_questions</code> <code>Dict[str, List[str]]</code> <p>Dict[str, List[str]] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import update_doc_question_memory\n&gt;&gt;&gt; update_doc_question_memory(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.lookup_source_path","title":"<code>lookup_source_path(question_inputs, topic_id)</code>","text":"<p>Look up source path.</p> <p>Parameters:</p> Name Type Description Default <code>question_inputs</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>topic_id</code> <code>str</code> <p>str value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import lookup_source_path\n&gt;&gt;&gt; lookup_source_path(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.maybe_select_seed_question","title":"<code>maybe_select_seed_question(base_inputs, turn_index, rng, used_seed_hashes, seed_topic_usage)</code>","text":"<p>Conditionally execute select seed question.</p> <p>Parameters:</p> Name Type Description Default <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>turn_index</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>rng</code> <code>Random</code> <p>random.Random value used by this operation.</p> required <code>used_seed_hashes</code> <code>set[str]</code> <p>set[str] value used by this operation.</p> required <code>seed_topic_usage</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>tuple[str, str]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import maybe_select_seed_question\n&gt;&gt;&gt; maybe_select_seed_question(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.load_seed_topics","title":"<code>load_seed_topics(path, project_root, config_dir, target_language='', seed_topics_variant='')</code>","text":"<p>Load seed topics.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Filesystem path used by this operation.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <code>config_dir</code> <code>Path</code> <p>Resolved project directory context.</p> required <code>target_language</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <code>seed_topics_variant</code> <code>str</code> <p>str value used by this operation.</p> <code>''</code> <p>Returns:</p> Type Description <code>Dict[str, List[str]]</code> <p>Dict[str, List[str]]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import load_seed_topics\n&gt;&gt;&gt; load_seed_topics(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.select_seed_candidate","title":"<code>select_seed_candidate(seed_topics, used_seed_hashes, seed_topic_usage, rng)</code>","text":"<p>Select seed candidate.</p> <p>Parameters:</p> Name Type Description Default <code>seed_topics</code> <code>Dict[str, List[str]]</code> <p>Dict[str, List[str]] value used by this operation.</p> required <code>used_seed_hashes</code> <code>set[str]</code> <p>set[str] value used by this operation.</p> required <code>seed_topic_usage</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <code>rng</code> <code>Random</code> <p>random.Random value used by this operation.</p> required <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>tuple[str, str]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import select_seed_candidate\n&gt;&gt;&gt; select_seed_candidate(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.build_used_seed_hashes","title":"<code>build_used_seed_hashes(ledger_entries)</code>","text":"<p>Build used seed hashes.</p> <p>Parameters:</p> Name Type Description Default <code>ledger_entries</code> <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]] value used by this operation.</p> required <p>Returns:</p> Type Description <code>set[str]</code> <p>set[str]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import build_used_seed_hashes\n&gt;&gt;&gt; build_used_seed_hashes(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.build_seed_topic_usage","title":"<code>build_seed_topic_usage(ledger_entries)</code>","text":"<p>Build seed topic usage.</p> <p>Parameters:</p> Name Type Description Default <code>ledger_entries</code> <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]] value used by this operation.</p> required <p>Returns:</p> Type Description <code>Dict[str, int]</code> <p>Dict[str, int]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import build_seed_topic_usage\n&gt;&gt;&gt; build_seed_topic_usage(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/sampling/#dlgforge.pipeline.sampling.update_seed_memory","title":"<code>update_seed_memory(qa_output, question_inputs, used_seed_hashes, seed_topic_usage)</code>","text":"<p>Update seed memory.</p> <p>Parameters:</p> Name Type Description Default <code>qa_output</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>question_inputs</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>used_seed_hashes</code> <code>set[str]</code> <p>set[str] value used by this operation.</p> required <code>seed_topic_usage</code> <code>Dict[str, int]</code> <p>Dict[str, int] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.sampling import update_seed_memory\n&gt;&gt;&gt; update_seed_memory(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/seed_topics_migration/","title":"<code>dlgforge.pipeline.seed_topics_migration</code>","text":"<p>Seed-topic migration utilities for legacy topic files.</p>"},{"location":"reference/dlgforge/pipeline/seed_topics_migration/#dlgforge.pipeline.seed_topics_migration.run_seeds_migrate","title":"<code>run_seeds_migrate(config_path, source_file='', dest_file='', overwrite=False)</code>","text":"<p>Run seed-topic migration using project config defaults.</p> <p>The command resolves source and destination paths from CLI overrides first, then from config, then from repository defaults.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to a configuration file.</p> required <code>source_file</code> <code>str</code> <p>Optional source seed file override (JSON or YAML).</p> <code>''</code> <code>dest_file</code> <code>str</code> <p>Optional destination YAML path override.</p> <code>''</code> <code>overwrite</code> <code>bool</code> <p>Whether to overwrite an existing destination file.</p> <code>False</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Migration summary with source path, destination path,</p> <code>Dict[str, Any]</code> <p>variant list, and topic counts.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If <code>config_path</code> or the resolved source file does not exist.</p> <code>ValueError</code> <p>If the resolved source path points to a directory.</p> <p>Side Effects / I/O:     - Reads config and source files from disk.     - Writes migrated YAML output to disk.     - Writes log entries.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.seed_topics_migration import run_seeds_migrate\n&gt;&gt;&gt; run_seeds_migrate(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/seed_topics_migration/#dlgforge.pipeline.seed_topics_migration.migrate_seed_topics_file","title":"<code>migrate_seed_topics_file(source_file, destination_file, overwrite=False)</code>","text":"<p>Convert a legacy seed-topic file into canonical YAML format.</p> <p>Parameters:</p> Name Type Description Default <code>source_file</code> <code>Path</code> <p>Legacy source file containing seed topics.</p> required <code>destination_file</code> <code>Path</code> <p>Output path for canonical YAML.</p> required <code>overwrite</code> <code>bool</code> <p>Whether to overwrite <code>destination_file</code> if it exists.</p> <code>False</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Migration summary with written files, variants, and topic</p> <code>Dict[str, Any]</code> <p>counts.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If source data is not a mapping or has no valid topics.</p> <code>FileExistsError</code> <p>If destination exists and <code>overwrite</code> is <code>False</code>.</p> <p>Side Effects / I/O:     - Reads source content from disk.     - Creates destination parent directories as needed.     - Writes canonical YAML to <code>destination_file</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from pathlib import Path\n&gt;&gt;&gt; from dlgforge.pipeline.seed_topics_migration import migrate_seed_topics_file\n&gt;&gt;&gt; migrate_seed_topics_file(Path(\"seed_topics.json\"), Path(\"data/seeds/topics.yaml\"), overwrite=True)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/state/","title":"<code>dlgforge.pipeline.state</code>","text":"<p>Run-state initialization and checkpoint helpers.</p>"},{"location":"reference/dlgforge/pipeline/state/#dlgforge.pipeline.state.init_run_state","title":"<code>init_run_state(paths, base_inputs, n_turns)</code>","text":"<p>Initialize run state.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>n_turns</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <p>Returns:</p> Type Description <code>Tuple[str, Dict[str, Any]]</code> <p>Tuple[str, Dict[str, Any]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.state import init_run_state\n&gt;&gt;&gt; init_run_state(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/state/#dlgforge.pipeline.state.checkpoint_run_state","title":"<code>checkpoint_run_state(paths, run_id, status, base_inputs, n_turns, turns, raw_results)</code>","text":"<p>Checkpoint run state.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>run_id</code> <code>str</code> <p>Identifier for run state tracking.</p> required <code>status</code> <code>str</code> <p>str value used by this operation.</p> required <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>n_turns</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>turns</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <code>raw_results</code> <code>List[Dict[str, Any]]</code> <p>Conversation or message data used during processing.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.state import checkpoint_run_state\n&gt;&gt;&gt; checkpoint_run_state(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/state/#dlgforge.pipeline.state.init_batched_run_state","title":"<code>init_batched_run_state(paths, base_inputs, n_turns, batch_size)</code>","text":"<p>Initialize batched run state.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>n_turns</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>batch_size</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <p>Returns:</p> Type Description <code>Tuple[str, Dict[str, Any]]</code> <p>Tuple[str, Dict[str, Any]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.state import init_batched_run_state\n&gt;&gt;&gt; init_batched_run_state(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/state/#dlgforge.pipeline.state.build_initial_batched_conversations","title":"<code>build_initial_batched_conversations(run_id, target_turns)</code>","text":"<p>Build initial batched conversations.</p> <p>Parameters:</p> Name Type Description Default <code>run_id</code> <code>str</code> <p>Identifier for run state tracking.</p> required <code>target_turns</code> <code>List[int]</code> <p>List[int] value used by this operation.</p> required <p>Returns:</p> Type Description <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]]: Constructed value derived from the provided inputs.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.state import build_initial_batched_conversations\n&gt;&gt;&gt; build_initial_batched_conversations(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/state/#dlgforge.pipeline.state.load_batched_conversations_from_state","title":"<code>load_batched_conversations_from_state(run_id, state, target_turns)</code>","text":"<p>Load batched conversations from state.</p> <p>Parameters:</p> Name Type Description Default <code>run_id</code> <code>str</code> <p>Identifier for run state tracking.</p> required <code>state</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>target_turns</code> <code>List[int]</code> <p>List[int] value used by this operation.</p> required <p>Returns:</p> Type Description <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]]: Loaded value parsed from upstream sources.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.state import load_batched_conversations_from_state\n&gt;&gt;&gt; load_batched_conversations_from_state(...)\n</code></pre>"},{"location":"reference/dlgforge/pipeline/state/#dlgforge.pipeline.state.checkpoint_batched_run_state","title":"<code>checkpoint_batched_run_state(paths, run_id, status, base_inputs, n_turns, batch_size, conversations)</code>","text":"<p>Checkpoint batched run state.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>OutputPaths</code> <p>Filesystem path used by this operation.</p> required <code>run_id</code> <code>str</code> <p>Identifier for run state tracking.</p> required <code>status</code> <code>str</code> <p>str value used by this operation.</p> required <code>base_inputs</code> <code>Dict[str, Any]</code> <p>Mapping payload for this operation.</p> required <code>n_turns</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>batch_size</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>conversations</code> <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.pipeline.state import checkpoint_batched_run_state\n&gt;&gt;&gt; checkpoint_batched_run_state(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/","title":"<code>dlgforge.tools</code>","text":"<p>Public exports for external tool integrations.</p>"},{"location":"reference/dlgforge/tools/#dlgforge.tools.SerperWebSearchClient","title":"<code>SerperWebSearchClient(num_results=5, timeout=30, api_key=None)</code>","text":"<p>Serper web search client.</p> <p>Parameters:</p> Name Type Description Default <code>num_results</code> <code>int</code> <p>int value used by this operation.</p> <code>5</code> <code>timeout</code> <code>int</code> <p>int value used by this operation.</p> <code>30</code> <code>api_key</code> <code>str | None</code> <p>str | None value used by this operation.</p> <code>None</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.web_search import SerperWebSearchClient\n&gt;&gt;&gt; SerperWebSearchClient(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/#dlgforge.tools.SerperWebSearchClient.search","title":"<code>search(query)</code>","text":"<p>Execute a web search query.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.web_search import SerperWebSearchClient\n&gt;&gt;&gt; instance = SerperWebSearchClient(...)\n&gt;&gt;&gt; instance.search(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/#dlgforge.tools.configure_retrieval","title":"<code>configure_retrieval(cfg, project_root)</code>","text":"<p>Configure retrieval.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import configure_retrieval\n&gt;&gt;&gt; configure_retrieval(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/#dlgforge.tools.get_vector_store","title":"<code>get_vector_store()</code>","text":"<p>Get vector store.</p> <p>Returns:</p> Name Type Description <code>KnowledgeVectorStore</code> <code>KnowledgeVectorStore</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import get_vector_store\n&gt;&gt;&gt; get_vector_store(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/#dlgforge.tools.vector_db_search","title":"<code>vector_db_search(query, k=None, use_reranker=False)</code>","text":"<p>Vector db search.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Input text.</p> required <code>k</code> <code>Optional[int]</code> <p>Numeric control value for processing behavior.</p> <code>None</code> <code>use_reranker</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import vector_db_search\n&gt;&gt;&gt; vector_db_search(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/","title":"<code>dlgforge.tools.retrieval</code>","text":"<p>Knowledge retrieval and vector-store utilities.</p>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.KnowledgeVectorStore","title":"<code>KnowledgeVectorStore(knowledge_dir, collection_name='dlgforge_knowledge', chunk_size=750, overlap=150, embedding_model_name=None, persist_dir=None, rebuild_index=False, skip_if_unchanged=True)</code>","text":"<p>Vector-store wrapper for indexing and retrieving knowledge passages.</p> <p>Parameters:</p> Name Type Description Default <code>knowledge_dir</code> <code>Path</code> <p>Path value used by this operation.</p> required <code>collection_name</code> <code>str</code> <p>str value used by this operation.</p> <code>'dlgforge_knowledge'</code> <code>chunk_size</code> <code>int</code> <p>int value used by this operation.</p> <code>750</code> <code>overlap</code> <code>int</code> <p>int value used by this operation.</p> <code>150</code> <code>embedding_model_name</code> <code>Optional[str]</code> <p>Optional[str] value used by this operation.</p> <code>None</code> <code>persist_dir</code> <code>Optional[Path]</code> <p>Optional[Path] value used by this operation.</p> <code>None</code> <code>rebuild_index</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <code>skip_if_unchanged</code> <code>bool</code> <p>bool value used by this operation.</p> <code>True</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import KnowledgeVectorStore\n&gt;&gt;&gt; KnowledgeVectorStore(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.KnowledgeVectorStore.similarity_search","title":"<code>similarity_search(query, k)</code>","text":"<p>Similarity search.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Input text.</p> required <code>k</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <p>Returns:</p> Type Description <code>List[Tuple[str, Dict[str, Any]]]</code> <p>List[Tuple[str, Dict[str, Any]]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import KnowledgeVectorStore\n&gt;&gt;&gt; instance = KnowledgeVectorStore(...)\n&gt;&gt;&gt; instance.similarity_search(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.KnowledgeVectorStore.similarity_search_with_ids","title":"<code>similarity_search_with_ids(query, k)</code>","text":"<p>Similarity search with ids.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Input text.</p> required <code>k</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <p>Returns:</p> Type Description <code>List[Tuple[str, Dict[str, Any], str]]</code> <p>List[Tuple[str, Dict[str, Any], str]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import KnowledgeVectorStore\n&gt;&gt;&gt; instance = KnowledgeVectorStore(...)\n&gt;&gt;&gt; instance.similarity_search_with_ids(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.KnowledgeVectorStore.list_sources","title":"<code>list_sources()</code>","text":"<p>List sources.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List[str]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import KnowledgeVectorStore\n&gt;&gt;&gt; instance = KnowledgeVectorStore(...)\n&gt;&gt;&gt; instance.list_sources(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.KnowledgeVectorStore.source_chunk_counts","title":"<code>source_chunk_counts()</code>","text":"<p>Source chunk counts.</p> <p>Returns:</p> Type Description <code>Dict[str, int]</code> <p>Dict[str, int]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import KnowledgeVectorStore\n&gt;&gt;&gt; instance = KnowledgeVectorStore(...)\n&gt;&gt;&gt; instance.source_chunk_counts(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.KnowledgeVectorStore.random_samples","title":"<code>random_samples(n, exclude_ids=None, rng=None)</code>","text":"<p>Random samples.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>exclude_ids</code> <code>Optional[set[str]]</code> <p>Optional[set[str]] value used by this operation.</p> <code>None</code> <code>rng</code> <code>Optional[Random]</code> <p>Optional[random.Random] value used by this operation.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Tuple[str, Dict[str, Any], str]]</code> <p>List[Tuple[str, Dict[str, Any], str]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import KnowledgeVectorStore\n&gt;&gt;&gt; instance = KnowledgeVectorStore(...)\n&gt;&gt;&gt; instance.random_samples(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.KnowledgeVectorStore.sample_by_sources","title":"<code>sample_by_sources(sources, n, exclude_ids=None, rng=None)</code>","text":"<p>Sample by sources.</p> <p>Parameters:</p> Name Type Description Default <code>sources</code> <code>set[str]</code> <p>set[str] value used by this operation.</p> required <code>n</code> <code>int</code> <p>Numeric control value for processing behavior.</p> required <code>exclude_ids</code> <code>Optional[set[str]]</code> <p>Optional[set[str]] value used by this operation.</p> <code>None</code> <code>rng</code> <code>Optional[Random]</code> <p>Optional[random.Random] value used by this operation.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Tuple[str, Dict[str, Any], str]]</code> <p>List[Tuple[str, Dict[str, Any], str]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import KnowledgeVectorStore\n&gt;&gt;&gt; instance = KnowledgeVectorStore(...)\n&gt;&gt;&gt; instance.sample_by_sources(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.configure_retrieval","title":"<code>configure_retrieval(cfg, project_root)</code>","text":"<p>Configure retrieval.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Dict[str, Any]</code> <p>Configuration mapping that controls runtime behavior.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import configure_retrieval\n&gt;&gt;&gt; configure_retrieval(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.get_vector_store","title":"<code>get_vector_store()</code>","text":"<p>Get vector store.</p> <p>Returns:</p> Name Type Description <code>KnowledgeVectorStore</code> <code>KnowledgeVectorStore</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import get_vector_store\n&gt;&gt;&gt; get_vector_store(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/retrieval/#dlgforge.tools.retrieval.vector_db_search","title":"<code>vector_db_search(query, k=None, use_reranker=False)</code>","text":"<p>Vector db search.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Input text.</p> required <code>k</code> <code>Optional[int]</code> <p>Numeric control value for processing behavior.</p> <code>None</code> <code>use_reranker</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read from or write to local filesystem artifacts.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.retrieval import vector_db_search\n&gt;&gt;&gt; vector_db_search(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/web_search/","title":"<code>dlgforge.tools.web_search</code>","text":"<p>Serper web-search integration helper.</p>"},{"location":"reference/dlgforge/tools/web_search/#dlgforge.tools.web_search.SerperWebSearchClient","title":"<code>SerperWebSearchClient(num_results=5, timeout=30, api_key=None)</code>","text":"<p>Serper web search client.</p> <p>Parameters:</p> Name Type Description Default <code>num_results</code> <code>int</code> <p>int value used by this operation.</p> <code>5</code> <code>timeout</code> <code>int</code> <p>int value used by this operation.</p> <code>30</code> <code>api_key</code> <code>str | None</code> <p>str | None value used by this operation.</p> <code>None</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Construction may raise when required dependencies or inputs are invalid.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Instantiate and use through documented public methods.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.web_search import SerperWebSearchClient\n&gt;&gt;&gt; SerperWebSearchClient(...)\n</code></pre>"},{"location":"reference/dlgforge/tools/web_search/#dlgforge.tools.web_search.SerperWebSearchClient.search","title":"<code>search(query)</code>","text":"<p>Execute a web search query.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Raised when validation or runtime requirements are not met.</p> <p>Side Effects / I/O:     - May perform network, model, or distributed runtime operations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.tools.web_search import SerperWebSearchClient\n&gt;&gt;&gt; instance = SerperWebSearchClient(...)\n&gt;&gt;&gt; instance.search(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/","title":"<code>dlgforge.utils</code>","text":"<p>Public exports for utility helpers.</p>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.env_flag","title":"<code>env_flag(name, default=False)</code>","text":"<p>Env flag.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>str value used by this operation.</p> required <code>default</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Boolean indicator describing the evaluated condition.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.env import env_flag\n&gt;&gt;&gt; env_flag(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.env_float","title":"<code>env_float(name, default)</code>","text":"<p>Env float.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>str value used by this operation.</p> required <code>default</code> <code>float</code> <p>float value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.env import env_float\n&gt;&gt;&gt; env_float(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.env_int","title":"<code>env_int(name, default)</code>","text":"<p>Env int.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>str value used by this operation.</p> required <code>default</code> <code>int</code> <p>int value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.env import env_int\n&gt;&gt;&gt; env_int(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.load_dotenv_files","title":"<code>load_dotenv_files(project_root)</code>","text":"<p>Load dotenv files.</p> <p>Parameters:</p> Name Type Description Default <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.env import load_dotenv_files\n&gt;&gt;&gt; load_dotenv_files(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.extract_json_object","title":"<code>extract_json_object(text)</code>","text":"<p>Extract json object.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Type Description <code>Optional[Dict[str, Any]]</code> <p>Optional[Dict[str, Any]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.json import extract_json_object\n&gt;&gt;&gt; extract_json_object(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.parse_json_object","title":"<code>parse_json_object(text)</code>","text":"<p>Parse json object.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.json import parse_json_object\n&gt;&gt;&gt; parse_json_object(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.strip_code_fences","title":"<code>strip_code_fences(text)</code>","text":"<p>Strip code fences.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.json import strip_code_fences\n&gt;&gt;&gt; strip_code_fences(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.setup_logging","title":"<code>setup_logging(logs_dir, level=logging.INFO)</code>","text":"<p>Setup logging.</p> <p>Parameters:</p> Name Type Description Default <code>logs_dir</code> <code>Path</code> <p>Path value used by this operation.</p> required <code>level</code> <code>int</code> <p>int value used by this operation.</p> <code>INFO</code> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.logging import setup_logging\n&gt;&gt;&gt; setup_logging(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.deep_merge","title":"<code>deep_merge(base, override)</code>","text":"<p>Deep merge.</p> <p>Parameters:</p> Name Type Description Default <code>base</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>override</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.merge import deep_merge\n&gt;&gt;&gt; deep_merge(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.resolve_path","title":"<code>resolve_path(path, project_root, config_dir=None)</code>","text":"<p>Resolve path from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Filesystem path used by this operation.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <code>config_dir</code> <code>Optional[Path]</code> <p>Resolved project directory context.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[Path]</code> <p>Optional[Path]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.merge import resolve_path\n&gt;&gt;&gt; resolve_path(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.hash_text","title":"<code>hash_text(text)</code>","text":"<p>Hash text.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.text import hash_text\n&gt;&gt;&gt; hash_text(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/#dlgforge.utils.render_template","title":"<code>render_template(template, values)</code>","text":"<p>Render template.</p> <p>Parameters:</p> Name Type Description Default <code>template</code> <code>str</code> <p>Input text.</p> required <code>values</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.text import render_template\n&gt;&gt;&gt; render_template(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/env/","title":"<code>dlgforge.utils.env</code>","text":"<p>Environment variable parsing and dotenv loading helpers.</p>"},{"location":"reference/dlgforge/utils/env/#dlgforge.utils.env.env_flag","title":"<code>env_flag(name, default=False)</code>","text":"<p>Env flag.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>str value used by this operation.</p> required <code>default</code> <code>bool</code> <p>bool value used by this operation.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Boolean indicator describing the evaluated condition.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.env import env_flag\n&gt;&gt;&gt; env_flag(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/env/#dlgforge.utils.env.env_int","title":"<code>env_int(name, default)</code>","text":"<p>Env int.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>str value used by this operation.</p> required <code>default</code> <code>int</code> <p>int value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.env import env_int\n&gt;&gt;&gt; env_int(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/env/#dlgforge.utils.env.env_float","title":"<code>env_float(name, default)</code>","text":"<p>Env float.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>str value used by this operation.</p> required <code>default</code> <code>float</code> <p>float value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.env import env_float\n&gt;&gt;&gt; env_float(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/env/#dlgforge.utils.env.load_dotenv_files","title":"<code>load_dotenv_files(project_root)</code>","text":"<p>Load dotenv files.</p> <p>Parameters:</p> Name Type Description Default <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.env import load_dotenv_files\n&gt;&gt;&gt; load_dotenv_files(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/json/","title":"<code>dlgforge.utils.json</code>","text":"<p>JSON extraction and parsing helpers for model outputs.</p>"},{"location":"reference/dlgforge/utils/json/#dlgforge.utils.json.strip_code_fences","title":"<code>strip_code_fences(text)</code>","text":"<p>Strip code fences.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.json import strip_code_fences\n&gt;&gt;&gt; strip_code_fences(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/json/#dlgforge.utils.json.extract_json_object","title":"<code>extract_json_object(text)</code>","text":"<p>Extract json object.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Type Description <code>Optional[Dict[str, Any]]</code> <p>Optional[Dict[str, Any]]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.json import extract_json_object\n&gt;&gt;&gt; extract_json_object(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/json/#dlgforge.utils.json.parse_json_object","title":"<code>parse_json_object(text)</code>","text":"<p>Parse json object.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.json import parse_json_object\n&gt;&gt;&gt; parse_json_object(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/logging/","title":"<code>dlgforge.utils.logging</code>","text":"<p>Structured logging setup helpers.</p>"},{"location":"reference/dlgforge/utils/logging/#dlgforge.utils.logging.setup_logging","title":"<code>setup_logging(logs_dir, level=logging.INFO)</code>","text":"<p>Setup logging.</p> <p>Parameters:</p> Name Type Description Default <code>logs_dir</code> <code>Path</code> <p>Path value used by this operation.</p> required <code>level</code> <code>int</code> <p>int value used by this operation.</p> <code>INFO</code> <p>Returns:</p> Name Type Description <code>None</code> <code>None</code> <p>No value is returned.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - May read environment variables or mutate process-level runtime state.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.logging import setup_logging\n&gt;&gt;&gt; setup_logging(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/merge/","title":"<code>dlgforge.utils.merge</code>","text":"<p>Dictionary merge and path resolution helpers.</p>"},{"location":"reference/dlgforge/utils/merge/#dlgforge.utils.merge.deep_merge","title":"<code>deep_merge(base, override)</code>","text":"<p>Deep merge.</p> <p>Parameters:</p> Name Type Description Default <code>base</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <code>override</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.merge import deep_merge\n&gt;&gt;&gt; deep_merge(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/merge/#dlgforge.utils.merge.resolve_path","title":"<code>resolve_path(path, project_root, config_dir=None)</code>","text":"<p>Resolve path from configuration.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Filesystem path used by this operation.</p> required <code>project_root</code> <code>Path</code> <p>Resolved project directory context.</p> required <code>config_dir</code> <code>Optional[Path]</code> <p>Resolved project directory context.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[Path]</code> <p>Optional[Path]: Resolved value after applying defaults and normalization rules.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.merge import resolve_path\n&gt;&gt;&gt; resolve_path(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/text/","title":"<code>dlgforge.utils.text</code>","text":"<p>Text hashing and template rendering helpers.</p>"},{"location":"reference/dlgforge/utils/text/#dlgforge.utils.text.hash_text","title":"<code>hash_text(text)</code>","text":"<p>Hash text.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Input text.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.text import hash_text\n&gt;&gt;&gt; hash_text(...)\n</code></pre>"},{"location":"reference/dlgforge/utils/text/#dlgforge.utils.text.render_template","title":"<code>render_template(template, values)</code>","text":"<p>Render template.</p> <p>Parameters:</p> Name Type Description Default <code>template</code> <code>str</code> <p>Input text.</p> required <code>values</code> <code>Dict[str, Any]</code> <p>Dict[str, Any] value used by this operation.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value produced by this API.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Propagates unexpected runtime errors from downstream calls.</p> <p>Side Effects / I/O:     - Primarily performs in-memory transformations.</p> <p>Preconditions / Invariants:     - Callers should provide arguments matching annotated types and expected data contracts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from dlgforge.utils.text import render_template\n&gt;&gt;&gt; render_template(...)\n</code></pre>"}]}